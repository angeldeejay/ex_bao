defmodule ExBao.Sys do
  @moduledoc """
  The system backend: mounts, policies, leases, seal, audit.

  Generated from OpenBao's OpenAPI specification by `mix bao.gen`. One
  function per endpoint: path parameters are arguments, every other field
  is an option, and the answer is the server's own JSON body. An option
  the endpoint does not accept raises `ArgumentError` rather than being
  sent and ignored.
  """

  use ExBao.Operation

  # ── generated by `mix bao.gen` from OpenBao 2.6.2 ──
  # Everything down to the closing marker is rewritten on every run. To
  # curate a function, move it above this block and keep its `@operation`:
  # the next run sees it there and stops generating it.

  @doc ~S"""
  The hash of the given string via the given audit backend

  `POST /v1/sys/audit-hash/{path}`

  ## Arguments

    * `path` — The name of the backend. Cannot be delimited. Example: "mysql"

  ## Options

    * `:input` (string) —
  """
  @operation "auditing-calculate-hash"
  @spec auditing_calculate_hash(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def auditing_calculate_hash(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/audit-hash/#{ExBao.Operation.escape_path(path)}",
      opts,
      body: [:input]
    )
  end

  @doc ~S"""
  Disable the audit device at the given path.

  `DELETE /v1/sys/audit/{path}` — needs `sudo`

  Enable or disable audit backends.

  ## Arguments

    * `path` — The name of the backend. Cannot be delimited. Example: "mysql"
  """
  @operation "auditing-disable-device"
  @spec auditing_disable_device(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def auditing_disable_device(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :delete,
      "sys/audit/#{ExBao.Operation.escape_path(path)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Disable auditing of the given request header.

  `DELETE /v1/sys/config/auditing/request-headers/{header}` — needs `sudo`

  Configures the headers sent to the audit logs.

  ## Arguments

    * `header` — 
  """
  @operation "auditing-disable-request-header"
  @spec auditing_disable_request_header(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def auditing_disable_request_header(server, header, opts \\ []) do
    ExBao.Operation.call(
      server,
      :delete,
      "sys/config/auditing/request-headers/#{ExBao.Operation.escape(header)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Enable a new audit device at the supplied path.

  `POST /v1/sys/audit/{path}` — needs `sudo`

  Enable or disable audit backends.

  ## Arguments

    * `path` — The name of the backend. Cannot be delimited. Example: "mysql"

  ## Options

    * `:description` (string) — User-friendly description for this audit backend.
    * `:local` (boolean) — Mark the mount as a local mount, which is not replicated and is unaffected by replication. Defaults to `false`.
    * `:options` (kvpairs) — Configuration options for the audit backend.
    * `:type` (string) — The type of the backend. Example: "mysql"
  """
  @operation "auditing-enable-device"
  @spec auditing_enable_device(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def auditing_enable_device(server, path, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/audit/#{ExBao.Operation.escape_path(path)}", opts,
      body: [:description, :local, :options, :type]
    )
  end

  @doc ~S"""
  Enable auditing of a header.

  `POST /v1/sys/config/auditing/request-headers/{header}` — needs `sudo`

  Configures the headers sent to the audit logs.

  ## Arguments

    * `header` — 

  ## Options

    * `:hmac` (boolean) —
  """
  @operation "auditing-enable-request-header"
  @spec auditing_enable_request_header(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def auditing_enable_request_header(server, header, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/config/auditing/request-headers/#{ExBao.Operation.escape(header)}",
      opts,
      body: [:hmac]
    )
  end

  @doc ~S"""
  List the enabled audit devices.

  `GET /v1/sys/audit` — needs `sudo`

  List the currently enabled audit backends.
  """
  @operation "auditing-list-enabled-devices"
  @spec auditing_list_enabled_devices(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def auditing_list_enabled_devices(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/audit", opts, [])
  end

  @doc ~S"""
  List the request headers that are configured to be audited.

  `GET /v1/sys/config/auditing/request-headers` — needs `sudo`

  Lists the headers configured to be audited.
  """
  @operation "auditing-list-request-headers"
  @spec auditing_list_request_headers(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def auditing_list_request_headers(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/config/auditing/request-headers", opts, [])
  end

  @doc ~S"""
  List the information for the given request header.

  `GET /v1/sys/config/auditing/request-headers/{header}` — needs `sudo`

  Configures the headers sent to the audit logs.

  ## Arguments

    * `header` — 
  """
  @operation "auditing-read-request-header-information"
  @spec auditing_read_request_header_information(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def auditing_read_request_header_information(server, header, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/config/auditing/request-headers/#{ExBao.Operation.escape(header)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Disable the auth method at the given auth path

  `DELETE /v1/sys/auth/{path}` — needs `sudo`

  Enable a new credential backend with a name.

  ## Arguments

    * `path` — The path to mount to. Cannot be delimited. Example: "user"
  """
  @operation "auth-disable-method"
  @spec auth_disable_method(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def auth_disable_method(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :delete,
      "sys/auth/#{ExBao.Operation.escape_path(path)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Enables a new auth method.

  `POST /v1/sys/auth/{path}` — needs `sudo`

  Enable a new credential backend with a name.

  ## Arguments

    * `path` — The path to mount to. Cannot be delimited. Example: "user"

  ## Options

    * `:config` (map) — Configuration for this mount, such as plugin_name.
    * `:description` (string) — User-friendly description for this credential backend.
    * `:external_entropy_access` (boolean) — Whether to give the mount access to OpenBao's external entropy. Defaults to `false`.
    * `:local` (boolean) — Mark the mount as a local mount, which is not replicated and is unaffected by replication. Defaults to `false`.
    * `:options` (kvpairs) — The options to pass into the backend. Should be a json object with string keys and values.
    * `:plugin_name` (string) — Name of the auth plugin to use based from the name in the plugin catalog.
    * `:plugin_version` (string) — The semantic version of the plugin to use.
    * `:seal_wrap` (boolean) — Whether to turn on seal wrapping for the mount. Defaults to `false`.
    * `:type` (string) — The type of the backend. Example: "userpass"
  """
  @operation "auth-enable-method"
  @spec auth_enable_method(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def auth_enable_method(server, path, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/auth/#{ExBao.Operation.escape_path(path)}", opts,
      body: [
        :config,
        :description,
        :external_entropy_access,
        :local,
        :options,
        :plugin_name,
        :plugin_version,
        :seal_wrap,
        :type
      ]
    )
  end

  @doc ~S"""
  List the currently enabled credential backends.

  `GET /v1/sys/auth`
  """
  @operation "auth-list-enabled-methods"
  @spec auth_list_enabled_methods(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def auth_list_enabled_methods(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/auth", opts, [])
  end

  @doc ~S"""
  Read the configuration of the auth engine at the given path.

  `GET /v1/sys/auth/{path}` — needs `sudo`

  Enable a new credential backend with a name.

  ## Arguments

    * `path` — The path to mount to. Cannot be delimited. Example: "user"
  """
  @operation "auth-read-configuration"
  @spec auth_read_configuration(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def auth_read_configuration(server, path, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/auth/#{ExBao.Operation.escape_path(path)}", opts, [])
  end

  @doc ~S"""
  Reads the given auth path's configuration.

  `GET /v1/sys/auth/{path}/tune` — needs `sudo`

  Tune the configuration parameters for an auth path.

  ## Arguments

    * `path` — Tune the configuration parameters for an auth path.
  """
  @operation "auth-read-tuning-information"
  @spec auth_read_tuning_information(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def auth_read_tuning_information(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/auth/#{ExBao.Operation.escape_path(path)}/tune",
      opts,
      []
    )
  end

  @doc ~S"""
  Tune configuration parameters for a given auth path.

  `POST /v1/sys/auth/{path}/tune` — needs `sudo`

  Tune the configuration parameters for an auth path.

  ## Arguments

    * `path` — Tune the configuration parameters for an auth path.

  ## Options

    * `:allowed_response_headers` (array) — A list of headers to whitelist and allow a plugin to set on responses.
    * `:audit_non_hmac_request_keys` (array) — The list of keys in the request data object that will not be HMAC'ed by audit devices.
    * `:audit_non_hmac_response_keys` (array) — The list of keys in the response data object that will not be HMAC'ed by audit devices.
    * `:default_lease_ttl` (string) — The default lease TTL for this mount.
    * `:description` (string) — User-friendly description for this credential backend.
    * `:listing_visibility` (string) — Determines the visibility of the mount in the UI-specific listing endpoint. Accepted value are 'unauth' and 'hidden', with the empty default ('') behaving like 'hidden'.
    * `:max_lease_ttl` (string) — The max lease TTL for this mount.
    * `:options` (kvpairs) — The options to pass into the backend. Should be a json object with string keys and values.
    * `:passthrough_request_headers` (array) — A list of headers to whitelist and pass from the request to the plugin.
    * `:plugin_version` (string) — The semantic version of the plugin to use.
    * `:token_type` (string) — The type of token to issue (service or batch).
    * `:user_lockout_config` (map) — The user lockout configuration to pass into the backend. Should be a json object with string keys and values.
  """
  @operation "auth-tune-configuration-parameters"
  @spec auth_tune_configuration_parameters(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def auth_tune_configuration_parameters(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/auth/#{ExBao.Operation.escape_path(path)}/tune",
      opts,
      body: [
        :allowed_response_headers,
        :audit_non_hmac_request_keys,
        :audit_non_hmac_response_keys,
        :default_lease_ttl,
        :description,
        :listing_visibility,
        :max_lease_ttl,
        :options,
        :passthrough_request_headers,
        :plugin_version,
        :token_type,
        :user_lockout_config
      ]
    )
  end

  @doc ~S"""
  Information about the host instance that this OpenBao server is running on.

  `GET /v1/sys/host-info`
  """
  @operation "collect-host-information"
  @spec collect_host_information(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def collect_host_information(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/host-info", opts, [])
  end

  @doc ~S"""
  reports in-flight requests

  `GET /v1/sys/in-flight-req`
  """
  @operation "collect-in-flight-request-information"
  @spec collect_in_flight_request_information(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def collect_in_flight_request_information(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/in-flight-req", opts, [])
  end

  @doc ~S"""
  Configure the automatic key rotation.

  `POST /v1/sys/rotate/config`

  ## Options

    * `:enabled` (boolean) — Whether automatic rotation is enabled.
    * `:interval` (seconds) — How long after installation of an active key term that the key will be automatically rotated.
    * `:max_operations` (int64) — The number of encryption operations performed before the barrier key is automatically rotated.
  """
  @operation "configure-encryption-key-config"
  @spec configure_encryption_key_config(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def configure_encryption_key_config(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/rotate/config", opts,
      body: [:enabled, :interval, :max_operations]
    )
  end

  @doc ~S"""
  Configure the automatic key rotation.

  `POST /v1/sys/rotate/keyring/config`

  ## Options

    * `:enabled` (boolean) — Whether automatic rotation is enabled.
    * `:interval` (seconds) — How long after installation of an active key term that the key will be automatically rotated.
    * `:max_operations` (int64) — The number of encryption operations performed before the barrier key is automatically rotated.
  """
  @operation "configure-rotate-keyring-config"
  @spec configure_rotate_keyring_config(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def configure_rotate_keyring_config(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/rotate/keyring/config", opts,
      body: [:enabled, :interval, :max_operations]
    )
  end

  @doc ~S"""
  Configure the CORS settings.

  `POST /v1/sys/config/cors` — needs `sudo`

  This path responds to the following HTTP methods. GET / Returns the configuration of the CORS setting. POST / Sets the comma-separated list of origins that can make cross-origin requests. DELETE / Clears the CORS configuration and disables acceptance of CORS requests.

  ## Options

    * `:allow_credentials` (boolean) — If true, the browser will be allowed to send credentials (e.g. kerberos authentication) with cross-origin requests.
    * `:allowed_headers` (array) — A comma-separated string or array of strings indicating headers that are allowed on cross-origin requests.
    * `:allowed_origins` (array) — A comma-separated string or array of strings indicating origins that may make cross-origin requests.
    * `:enable` (boolean) — Enables or disables CORS headers on requests.
  """
  @operation "cors-configure"
  @spec cors_configure(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def cors_configure(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/config/cors", opts,
      body: [:allow_credentials, :allowed_headers, :allowed_origins, :enable]
    )
  end

  @doc ~S"""
  Remove any CORS settings.

  `DELETE /v1/sys/config/cors` — needs `sudo`

  This path responds to the following HTTP methods. GET / Returns the configuration of the CORS setting. POST / Sets the comma-separated list of origins that can make cross-origin requests. DELETE / Clears the CORS configuration and disables acceptance of CORS requests.
  """
  @operation "cors-delete-configuration"
  @spec cors_delete_configuration(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def cors_delete_configuration(server, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/config/cors", opts, [])
  end

  @doc ~S"""
  Return the current CORS settings.

  `GET /v1/sys/config/cors` — needs `sudo`

  This path responds to the following HTTP methods. GET / Returns the configuration of the CORS setting. POST / Sets the comma-separated list of origins that can make cross-origin requests. DELETE / Clears the CORS configuration and disables acceptance of CORS requests.
  """
  @operation "cors-read-configuration"
  @spec cors_read_configuration(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def cors_read_configuration(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/config/cors", opts, [])
  end

  @doc ~S"""
  Decodes the encoded token with the otp.

  `POST /v1/sys/decode-token` — needs no token

  Decodes encoded root token generated through `sys/generate-root-token` or unauthenticated `sys/generate-root` endpoint

  ## Options

    * `:encoded_token` (string) — Specifies the encoded token (result from generate-root-token call).
    * `:otp` (string) — Specifies the otp code used for decoding.
  """
  @operation "decode-root-token"
  @spec decode_root_token(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def decode_root_token(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/decode-token", opts, body: [:encoded_token, :otp])
  end

  @doc ~S"""
  Generate a hash sum for input data

  `POST /v1/sys/tools/hash`

  ## Options

    * `:algorithm` (string) — Algorithm to use (POST body parameter). Valid values are: * sha2-224 * sha2-256 * sha2-384 * sha2-512 Defaults to "sha2-256". Defaults to `"sha2-256"`.
    * `:format` (string) — Encoding format to use. Can be "hex" or "base64". Defaults to "hex". Defaults to `"hex"`.
    * `:input` (string) — The base64-encoded input data
    * `:urlalgorithm` (string) — Algorithm to use (POST URL parameter)
  """
  @operation "generate-hash"
  @spec generate_hash(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def generate_hash(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/tools/hash", opts,
      body: [:algorithm, :format, :input, :urlalgorithm]
    )
  end

  @doc ~S"""
  Generate a hash sum for input data

  `POST /v1/sys/tools/hash/{urlalgorithm}`

  ## Arguments

    * `urlalgorithm` — Algorithm to use (POST URL parameter)

  ## Options

    * `:algorithm` (string) — Algorithm to use (POST body parameter). Valid values are: * sha2-224 * sha2-256 * sha2-384 * sha2-512 Defaults to "sha2-256". Defaults to `"sha2-256"`.
    * `:format` (string) — Encoding format to use. Can be "hex" or "base64". Defaults to "hex". Defaults to `"hex"`.
    * `:input` (string) — The base64-encoded input data
  """
  @operation "generate-hash-with-algorithm"
  @spec generate_hash_with_algorithm(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def generate_hash_with_algorithm(server, urlalgorithm, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/tools/hash/#{ExBao.Operation.escape(urlalgorithm)}",
      opts,
      body: [:algorithm, :format, :input]
    )
  end

  @doc ~S"""
  Generate random bytes

  `POST /v1/sys/tools/random`

  ## Options

    * `:bytes` (integer) — The number of bytes to generate (POST body parameter). Defaults to 32 (256 bits). Defaults to `32`.
    * `:format` (string) — Encoding format to use. Can be "hex" or "base64". Defaults to "base64". Defaults to `"base64"`.
    * `:source` (string) — Which system to source random data from, ether "platform", "seal", or "all". Defaults to `"platform"`.
    * `:urlbytes` (string) — The number of bytes to generate (POST URL parameter)
  """
  @operation "generate-random"
  @spec generate_random(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def generate_random(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/tools/random", opts,
      body: [:bytes, :format, :source, :urlbytes]
    )
  end

  @doc ~S"""
  Generate random bytes

  `POST /v1/sys/tools/random/{urlbytes}`

  ## Arguments

    * `urlbytes` — The number of bytes to generate (POST URL parameter)

  ## Options

    * `:bytes` (integer) — The number of bytes to generate (POST body parameter). Defaults to 32 (256 bits). Defaults to `32`.
    * `:format` (string) — Encoding format to use. Can be "hex" or "base64". Defaults to "base64". Defaults to `"base64"`.
    * `:source` (string) — Which system to source random data from, ether "platform", "seal", or "all". Defaults to `"platform"`.
  """
  @operation "generate-random-with-bytes"
  @spec generate_random_with_bytes(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def generate_random_with_bytes(server, urlbytes, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/tools/random/#{ExBao.Operation.escape(urlbytes)}",
      opts,
      body: [:bytes, :format, :source]
    )
  end

  @doc ~S"""
  Generate random bytes

  `POST /v1/sys/tools/random/{source}`

  ## Arguments

    * `source` — Which system to source random data from, ether "platform", "seal", or "all".

  ## Options

    * `:bytes` (integer) — The number of bytes to generate (POST body parameter). Defaults to 32 (256 bits). Defaults to `32`.
    * `:format` (string) — Encoding format to use. Can be "hex" or "base64". Defaults to "base64". Defaults to `"base64"`.
    * `:urlbytes` (string) — The number of bytes to generate (POST URL parameter)
  """
  @operation "generate-random-with-source"
  @spec generate_random_with_source(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def generate_random_with_source(server, source, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/tools/random/#{ExBao.Operation.escape(source)}",
      opts,
      body: [:bytes, :format, :urlbytes]
    )
  end

  @doc ~S"""
  Generate random bytes

  `POST /v1/sys/tools/random/{source}/{urlbytes}`

  ## Arguments

    * `source` — Which system to source random data from, ether "platform", "seal", or "all".
    * `urlbytes` — The number of bytes to generate (POST URL parameter)

  ## Options

    * `:bytes` (integer) — The number of bytes to generate (POST body parameter). Defaults to 32 (256 bits). Defaults to `32`.
    * `:format` (string) — Encoding format to use. Can be "hex" or "base64". Defaults to "base64". Defaults to `"base64"`.
  """
  @operation "generate-random-with-source-and-bytes"
  @spec generate_random_with_source_and_bytes(ExBao.server(), String.t(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def generate_random_with_source_and_bytes(server, source, urlbytes, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/tools/random/#{ExBao.Operation.escape(source)}/#{ExBao.Operation.escape(urlbytes)}",
      opts,
      body: [:bytes, :format]
    )
  end

  @doc ~S"""
  Cancel root token generation.

  `DELETE /v1/sys/generate-root-token/attempt`

  Reads status, initializes, or cancels a root token generation process.
  """
  @operation "generate-root-token:delete"
  @spec generate_root_token_delete(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def generate_root_token_delete(server, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/generate-root-token/attempt", opts, [])
  end

  @doc ~S"""
  Read the status of root token generation.

  `GET /v1/sys/generate-root-token/attempt`

  Reads status, initializes, or cancels a root token generation process.
  """
  @operation "generate-root-token:get"
  @spec generate_root_token_get(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def generate_root_token_get(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/generate-root-token/attempt", opts, [])
  end

  @doc ~S"""
  Initialize root token generation.

  `POST /v1/sys/generate-root-token/attempt`

  Reads status, initializes, or cancels a root token generation process.

  ## Options

    * `:otp` (string) — One-time password for encoding the root token.
    * `:pgp_key` (string) — PGP key for encrypting the root token.
  """
  @operation "generate-root-token:post"
  @spec generate_root_token_post(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def generate_root_token_post(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/generate-root-token/attempt", opts,
      body: [:otp, :pgp_key]
    )
  end

  @doc ~S"""
  Check the HA status of an OpenBao cluster

  `GET /v1/sys/ha-status`

  Provides information about the nodes in an HA cluster.
  """
  @operation "ha-status"
  @spec ha_status(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def ha_status(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/ha-status", opts, [])
  end

  @doc ~S"""
  Initialize a new OpenBao instance.

  `POST /v1/sys/init` — needs no token

  Initializes or returns the initialization status of OpenBao.

  ## Options

    * `:pgp_keys` (array) — Specifies an array of PGP public keys used to encrypt the output unseal keys. Ordering is preserved. The keys must be base64-encoded from their original binary representation. The size of this array must be the same as `secret_shares`.
    * `:recovery_pgp_keys` (array) — Specifies an array of PGP public keys used to encrypt the output recovery keys. Ordering is preserved. The keys must be base64-encoded from their original binary representation. The size of this array must be the same as `recovery_shares`.
    * `:recovery_shares` (integer) — Specifies the number of shares to split the recovery key into.
    * `:recovery_threshold` (integer) — Specifies the number of shares required to reconstruct the recovery key. This must be less than or equal to `recovery_shares`.
    * `:root_token_pgp_key` (string) — Specifies a PGP public key used to encrypt the initial root token. The key must be base64-encoded from its original binary representation.
    * `:secret_shares` (integer) — Specifies the number of shares to split the unseal key into.
    * `:secret_threshold` (integer) — Specifies the number of shares required to reconstruct the unseal key. This must be less than or equal secret_shares. If using OpenBao HSM with auto-unsealing, this value must be the same as `secret_shares`.
  """
  @operation "initialize-system"
  @spec initialize_system(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def initialize_system(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/init", opts,
      body: [
        :pgp_keys,
        :recovery_pgp_keys,
        :recovery_shares,
        :recovery_threshold,
        :root_token_pgp_key,
        :secret_shares,
        :secret_threshold
      ]
    )
  end

  @doc ~S"""
  Backwards compatibility is not guaranteed for this API

  `GET /v1/sys/internal/counters/entities`

  Count of active entities in this OpenBao cluster.
  """
  @operation "internal-count-entities"
  @spec internal_count_entities(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def internal_count_entities(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/internal/counters/entities", opts, [])
  end

  @doc ~S"""
  Backwards compatibility is not guaranteed for this API

  `GET /v1/sys/internal/counters/requests`

  Currently unsupported. Previously, count of requests seen by this OpenBao cluster over time.
  """
  @operation "internal-count-requests"
  @spec internal_count_requests(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def internal_count_requests(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/internal/counters/requests", opts, [])
  end

  @doc ~S"""
  Backwards compatibility is not guaranteed for this API

  `GET /v1/sys/internal/counters/tokens`

  Count of active tokens in this OpenBao cluster.
  """
  @operation "internal-count-tokens"
  @spec internal_count_tokens(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def internal_count_tokens(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/internal/counters/tokens", opts, [])
  end

  @doc ~S"""
  Generate an OpenAPI 3 document of all mounted paths.

  `GET /v1/sys/internal/specs/openapi` — needs no token

  ## Options

    * `:generic_mount_paths` (boolean) — Use generic mount paths Defaults to `false`.
  """
  @operation "internal-generate-open-api-document"
  @spec internal_generate_open_api_document(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def internal_generate_open_api_document(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/internal/specs/openapi", opts,
      query: [:generic_mount_paths]
    )
  end

  @doc ~S"""
  Generate an OpenAPI 3 document of all mounted paths.

  `POST /v1/sys/internal/specs/openapi` — needs no token

  ## Options

    * `:context` (string) — Context string appended to every operationId
    * `:generic_mount_paths` (boolean) — Use generic mount paths Defaults to `false`.
  """
  @operation "internal-generate-open-api-document-with-parameters"
  @spec internal_generate_open_api_document_with_parameters(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def internal_generate_open_api_document_with_parameters(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/internal/specs/openapi", opts,
      body: [:context],
      query: [:generic_mount_paths]
    )
  end

  @doc ~S"""
  Expose all request information to the caller

  `GET /v1/sys/internal/inspect/request` — needs `sudo`

  Outputs information contained within a request to a plugin.
  """
  @operation "internal-inspect-request"
  @spec internal_inspect_request(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def internal_inspect_request(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/internal/inspect/request", opts, [])
  end

  @doc ~S"""
  Expose the route entry and mount entry tables present in the router

  `GET /v1/sys/internal/inspect/router/{tag}` — needs `sudo`

  Information on the entries in each of the trees in the router. Inspectable trees are uuid, accessor, storage, and root.

  ## Arguments

    * `tag` — Name of subtree being observed
  """
  @operation "internal-inspect-router"
  @spec internal_inspect_router(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def internal_inspect_router(server, tag, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/internal/inspect/router/#{ExBao.Operation.escape(tag)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Lists all enabled and visible auth and secrets mounts.

  `GET /v1/sys/internal/ui/mounts` — needs no token

  Information about mounts returned according to their tuned visibility. Internal API; its location, inputs, and outputs may change.
  """
  @operation "internal-ui-list-enabled-visible-mounts"
  @spec internal_ui_list_enabled_visible_mounts(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def internal_ui_list_enabled_visible_mounts(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/internal/ui/mounts", opts, [])
  end

  @doc ~S"""
  Backwards compatibility is not guaranteed for this API

  `GET /v1/sys/internal/ui/namespaces` — needs no token

  Information about visible child namespaces. Internal API; its location, inputs, and outputs may change.
  """
  @operation "internal-ui-list-namespaces"
  @spec internal_ui_list_namespaces(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def internal_ui_list_namespaces(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/internal/ui/namespaces", opts, [])
  end

  @doc ~S"""
  Return information about the given mount.

  `GET /v1/sys/internal/ui/mounts/{path}` — needs no token

  Information about mounts returned according to their tuned visibility. Internal API; its location, inputs, and outputs may change.

  ## Arguments

    * `path` — The path of the mount.
  """
  @operation "internal-ui-read-mount-information"
  @spec internal_ui_read_mount_information(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def internal_ui_read_mount_information(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/internal/ui/mounts/#{ExBao.Operation.escape_path(path)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Backwards compatibility is not guaranteed for this API

  `GET /v1/sys/internal/ui/resultant-acl`

  Information about a token's resultant ACL. Internal API; its location, inputs, and outputs may change.
  """
  @operation "internal-ui-read-resultant-acl"
  @spec internal_ui_read_resultant_acl(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def internal_ui_read_resultant_acl(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/internal/ui/resultant-acl", opts, [])
  end

  @doc ~S"""
  Returns the high availability status and current leader instance of OpenBao.

  `GET /v1/sys/leader` — needs no token

  Check the high availability status and current leader of OpenBao
  """
  @operation "leader-status"
  @spec leader_status(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def leader_status(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/leader", opts, [])
  end

  @doc ~S"""
  Count of leases associated with this OpenBao cluster

  `GET /v1/sys/leases/count`
  """
  @operation "leases-count"
  @spec leases_count(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def leases_count(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/leases/count", opts, [])
  end

  @doc ~S"""
  Revokes all secrets or tokens generated under a given prefix immediately

  `POST /v1/sys/leases/revoke-force/{prefix}` — needs `sudo`

  Revoke all secrets generated in a given prefix, ignoring errors.

  ## Arguments

    * `prefix` — The path to revoke keys under. Example: "prod/aws/ops"
  """
  @operation "leases-force-revoke-lease-with-prefix2"
  @spec leases_force_revoke_lease_with_prefix2(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def leases_force_revoke_lease_with_prefix2(server, prefix, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/leases/revoke-force/#{ExBao.Operation.escape_path(prefix)}",
      opts,
      []
    )
  end

  @doc ~S"""
  List leases associated with this OpenBao cluster

  `GET /v1/sys/leases` — needs `sudo`
  """
  @operation "leases-list"
  @spec leases_list(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def leases_list(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/leases", opts, [])
  end

  @doc ~S"""
  View or list lease metadata.

  `GET /v1/sys/leases/lookup/` — needs `sudo`
  """
  @operation "leases-look-up"
  @spec leases_look_up(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def leases_look_up(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/leases/lookup/", opts,
      fixed_query: %{"list" => "true"}
    )
  end

  @doc ~S"""
  View or list lease metadata.

  `GET /v1/sys/leases/lookup/{prefix}` — needs `sudo`

  ## Arguments

    * `prefix` — The path to list leases under. Example: "aws/creds/deploy"
  """
  @operation "leases-look-up-with-prefix"
  @spec leases_look_up_with_prefix(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def leases_look_up_with_prefix(server, prefix, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/leases/lookup/#{ExBao.Operation.escape_path(prefix)}",
      opts,
      fixed_query: %{"list" => "true"}
    )
  end

  @doc ~S"""
  View or list lease metadata.

  `POST /v1/sys/leases/lookup`

  ## Options

    * `:lease_id` (string) — The lease identifier to renew. This is included with a lease.
  """
  @operation "leases-read-lease"
  @spec leases_read_lease(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def leases_read_lease(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/leases/lookup", opts, body: [:lease_id])
  end

  @doc ~S"""
  Renews a lease, requesting to extend the lease.

  `POST /v1/sys/leases/renew/{url_lease_id}`

  Renew a lease on a secret

  ## Arguments

    * `url_lease_id` — The lease identifier to renew. This is included with a lease.

  ## Options

    * `:increment` (seconds) — The desired increment in seconds to the lease
    * `:lease_id` (string) — The lease identifier to renew. This is included with a lease.
  """
  @operation "leases-renew-lease"
  @spec leases_renew_lease(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def leases_renew_lease(server, url_lease_id, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/leases/renew/#{ExBao.Operation.escape_path(url_lease_id)}",
      opts,
      body: [:increment, :lease_id]
    )
  end

  @doc ~S"""
  Renews a lease, requesting to extend the lease.

  `POST /v1/sys/leases/renew`

  Renew a lease on a secret

  ## Options

    * `:increment` (seconds) — The desired increment in seconds to the lease
    * `:lease_id` (string) — The lease identifier to renew. This is included with a lease.
    * `:url_lease_id` (string) — The lease identifier to renew. This is included with a lease.
  """
  @operation "leases-renew-lease2"
  @spec leases_renew_lease2(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def leases_renew_lease2(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/leases/renew", opts,
      body: [:increment, :lease_id, :url_lease_id]
    )
  end

  @doc ~S"""
  Revokes a lease immediately.

  `POST /v1/sys/leases/revoke/{url_lease_id}`

  Revoke a leased secret immediately

  ## Arguments

    * `url_lease_id` — The lease identifier to renew. This is included with a lease.

  ## Options

    * `:lease_id` (string) — The lease identifier to renew. This is included with a lease.
    * `:sync` (boolean) — Whether or not to perform the revocation synchronously Defaults to `true`.
  """
  @operation "leases-revoke-lease"
  @spec leases_revoke_lease(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def leases_revoke_lease(server, url_lease_id, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/leases/revoke/#{ExBao.Operation.escape_path(url_lease_id)}",
      opts,
      body: [:lease_id, :sync]
    )
  end

  @doc ~S"""
  Revokes a lease immediately.

  `POST /v1/sys/leases/revoke`

  Revoke a leased secret immediately

  ## Options

    * `:lease_id` (string) — The lease identifier to renew. This is included with a lease.
    * `:sync` (boolean) — Whether or not to perform the revocation synchronously Defaults to `true`.
    * `:url_lease_id` (string) — The lease identifier to renew. This is included with a lease.
  """
  @operation "leases-revoke-lease2"
  @spec leases_revoke_lease2(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def leases_revoke_lease2(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/leases/revoke", opts,
      body: [:lease_id, :sync, :url_lease_id]
    )
  end

  @doc ~S"""
  Revokes all secrets (via a lease ID prefix) or tokens (via the tokens' path property) generated under a given prefix immediately.

  `POST /v1/sys/leases/revoke-prefix/{prefix}` — needs `sudo`

  Revoke all secrets generated in a given prefix

  ## Arguments

    * `prefix` — The path to revoke keys under. Example: "prod/aws/ops"

  ## Options

    * `:sync` (boolean) — Whether or not to perform the revocation synchronously Defaults to `true`.
  """
  @operation "leases-revoke-lease-with-prefix2"
  @spec leases_revoke_lease_with_prefix2(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def leases_revoke_lease_with_prefix2(server, prefix, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/leases/revoke-prefix/#{ExBao.Operation.escape_path(prefix)}",
      opts,
      body: [:sync]
    )
  end

  @doc ~S"""
  This endpoint performs cleanup tasks that can be run if certain error conditions have occurred.

  `POST /v1/sys/leases/tidy`
  """
  @operation "leases-tidy"
  @spec leases_tidy(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def leases_tidy(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/leases/tidy", opts, [])
  end

  @doc ~S"""
  Report the locked user count metrics, for this namespace and all child namespaces.

  `GET /v1/sys/locked-users`

  Report the locked user count metrics
  """
  @operation "locked-users-list"
  @spec locked_users_list(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def locked_users_list(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/locked-users", opts, [])
  end

  @doc ~S"""
  Unlocks the user with given mount_accessor and alias_identifier

  `POST /v1/sys/locked-users/{mount_accessor}/unlock/{alias_identifier}`

  Unlock the locked user with given mount_accessor and alias_identifier.

  ## Arguments

    * `mount_accessor` — MountAccessor is the identifier of the mount entry to which the user belongs
    * `alias_identifier` — It is the name of the alias (user). For example, if the alias belongs to userpass backend, the name should be a valid username within userpass auth method. If the alias belongs to an approle auth method, the name should be a valid RoleID
  """
  @operation "locked-users-unlock"
  @spec locked_users_unlock(ExBao.server(), String.t(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def locked_users_unlock(server, mount_accessor, alias_identifier, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/locked-users/#{ExBao.Operation.escape(mount_accessor)}/unlock/#{ExBao.Operation.escape(alias_identifier)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Read the log level for all existing loggers.

  `GET /v1/sys/loggers`
  """
  @operation "loggers-read-verbosity-level"
  @spec loggers_read_verbosity_level(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def loggers_read_verbosity_level(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/loggers", opts, [])
  end

  @doc ~S"""
  Read the log level for a single logger.

  `GET /v1/sys/loggers/{name}`

  ## Arguments

    * `name` — The name of the logger to be modified.
  """
  @operation "loggers-read-verbosity-level-for"
  @spec loggers_read_verbosity_level_for(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def loggers_read_verbosity_level_for(server, name, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/loggers/#{ExBao.Operation.escape(name)}", opts, [])
  end

  @doc ~S"""
  Revert the all loggers to use log level provided in config.

  `DELETE /v1/sys/loggers`
  """
  @operation "loggers-revert-verbosity-level"
  @spec loggers_revert_verbosity_level(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def loggers_revert_verbosity_level(server, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/loggers", opts, [])
  end

  @doc ~S"""
  Revert a single logger to use log level provided in config.

  `DELETE /v1/sys/loggers/{name}`

  ## Arguments

    * `name` — The name of the logger to be modified.
  """
  @operation "loggers-revert-verbosity-level-for"
  @spec loggers_revert_verbosity_level_for(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def loggers_revert_verbosity_level_for(server, name, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/loggers/#{ExBao.Operation.escape(name)}", opts, [])
  end

  @doc ~S"""
  Modify the log level for all existing loggers.

  `POST /v1/sys/loggers`

  ## Options

    * `:level` (string) — Log verbosity level. Supported values (in order of detail) are "trace", "debug", "info", "warn", and "error".
  """
  @operation "loggers-update-verbosity-level"
  @spec loggers_update_verbosity_level(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def loggers_update_verbosity_level(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/loggers", opts, body: [:level])
  end

  @doc ~S"""
  Modify the log level of a single logger.

  `POST /v1/sys/loggers/{name}`

  ## Arguments

    * `name` — The name of the logger to be modified.

  ## Options

    * `:level` (string) — Log verbosity level. Supported values (in order of detail) are "trace", "debug", "info", "warn", and "error".
  """
  @operation "loggers-update-verbosity-level-for"
  @spec loggers_update_verbosity_level_for(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def loggers_update_verbosity_level_for(server, name, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/loggers/#{ExBao.Operation.escape(name)}", opts,
      body: [:level]
    )
  end

  @doc ~S"""
  Export the metrics aggregated for telemetry purpose.

  `GET /v1/sys/metrics`

  ## Options

    * `:format` (string) — Format to export metrics into. Currently accepts only "prometheus".
  """
  @operation "metrics"
  @spec metrics(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def metrics(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/metrics", opts, query: [:format])
  end

  @doc ~S"""
  Validates the login for the given MFA methods. Upon successful validation, it returns an auth response containing the client token

  `POST /v1/sys/mfa/validate` — needs no token

  ## Options

    * `:mfa_payload` (map, required) — A map from MFA method ID to a slice of passcodes or an empty slice if the method does not use passcodes
    * `:mfa_request_id` (string, required) — ID for this MFA request
  """
  @operation "mfa-validate"
  @spec mfa_validate(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def mfa_validate(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/mfa/validate", opts,
      body: [:mfa_payload, :mfa_request_id],
      required: [:mfa_payload, :mfa_request_id]
    )
  end

  @doc ~S"""
  `monitor`

  `GET /v1/sys/monitor`

  ## Options

    * `:log_format` (string) — Output format of logs. Supported values are "standard" and "json". The default is "standard". Defaults to `"standard"`.
    * `:log_level` (string) — Log level to view system logs at. Currently supported values are "trace", "debug", "info", "warn", "error".
  """
  @operation "monitor"
  @spec monitor(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def monitor(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/monitor", opts, query: [:log_format, :log_level])
  end

  @doc ~S"""
  Disable the mount point specified at the given path.

  `DELETE /v1/sys/mounts/{path}`

  Mount a new backend at a new path.

  ## Arguments

    * `path` — The path to mount to. Example: "aws/east"
  """
  @operation "mounts-disable-secrets-engine"
  @spec mounts_disable_secrets_engine(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def mounts_disable_secrets_engine(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :delete,
      "sys/mounts/#{ExBao.Operation.escape_path(path)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Enable a new secrets engine at the given path.

  `POST /v1/sys/mounts/{path}`

  Mount a new backend at a new path.

  ## Arguments

    * `path` — The path to mount to. Example: "aws/east"

  ## Options

    * `:config` (map) — Configuration for this mount, such as default_lease_ttl and max_lease_ttl.
    * `:description` (string) — User-friendly description for this mount.
    * `:external_entropy_access` (boolean) — Whether to give the mount access to OpenBao's external entropy. Defaults to `false`.
    * `:local` (boolean) — Mark the mount as a local mount, which is not replicated and is unaffected by replication. Defaults to `false`.
    * `:options` (kvpairs) — The options to pass into the backend. Should be a json object with string keys and values.
    * `:plugin_name` (string) — Name of the plugin to mount based from the name registered in the plugin catalog.
    * `:plugin_version` (string) — The semantic version of the plugin to use.
    * `:seal_wrap` (boolean) — Whether to turn on seal wrapping for the mount. Defaults to `false`.
    * `:type` (string) — The type of the backend. Example: "passthrough"
  """
  @operation "mounts-enable-secrets-engine"
  @spec mounts_enable_secrets_engine(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def mounts_enable_secrets_engine(server, path, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/mounts/#{ExBao.Operation.escape_path(path)}", opts,
      body: [
        :config,
        :description,
        :external_entropy_access,
        :local,
        :options,
        :plugin_name,
        :plugin_version,
        :seal_wrap,
        :type
      ]
    )
  end

  @doc ~S"""
  List the currently mounted backends.

  `GET /v1/sys/mounts`
  """
  @operation "mounts-list-secrets-engines"
  @spec mounts_list_secrets_engines(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def mounts_list_secrets_engines(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/mounts", opts, [])
  end

  @doc ~S"""
  Read the configuration of the secret engine at the given path.

  `GET /v1/sys/mounts/{path}`

  Mount a new backend at a new path.

  ## Arguments

    * `path` — The path to mount to. Example: "aws/east"
  """
  @operation "mounts-read-configuration"
  @spec mounts_read_configuration(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def mounts_read_configuration(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/mounts/#{ExBao.Operation.escape_path(path)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Tune backend configuration parameters for this mount.

  `GET /v1/sys/mounts/{path}/tune`

  ## Arguments

    * `path` — The path to mount to. Example: "aws/east"
  """
  @operation "mounts-read-tuning-information"
  @spec mounts_read_tuning_information(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def mounts_read_tuning_information(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/mounts/#{ExBao.Operation.escape_path(path)}/tune",
      opts,
      []
    )
  end

  @doc ~S"""
  Tune backend configuration parameters for this mount.

  `POST /v1/sys/mounts/{path}/tune`

  ## Arguments

    * `path` — The path to mount to. Example: "aws/east"

  ## Options

    * `:allowed_response_headers` (array) — A list of headers to whitelist and allow a plugin to set on responses.
    * `:audit_non_hmac_request_keys` (array) — The list of keys in the request data object that will not be HMAC'ed by audit devices.
    * `:audit_non_hmac_response_keys` (array) — The list of keys in the response data object that will not be HMAC'ed by audit devices.
    * `:default_lease_ttl` (string) — The default lease TTL for this mount.
    * `:description` (string) — User-friendly description for this credential backend.
    * `:listing_visibility` (string) — Determines the visibility of the mount in the UI-specific listing endpoint. Accepted value are 'unauth' and 'hidden', with the empty default ('') behaving like 'hidden'.
    * `:max_lease_ttl` (string) — The max lease TTL for this mount.
    * `:options` (kvpairs) — The options to pass into the backend. Should be a json object with string keys and values.
    * `:passthrough_request_headers` (array) — A list of headers to whitelist and pass from the request to the plugin.
    * `:plugin_version` (string) — The semantic version of the plugin to use.
    * `:token_type` (string) — The type of token to issue (service or batch).
    * `:user_lockout_config` (map) — The user lockout configuration to pass into the backend. Should be a json object with string keys and values.
  """
  @operation "mounts-tune-configuration-parameters"
  @spec mounts_tune_configuration_parameters(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def mounts_tune_configuration_parameters(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/mounts/#{ExBao.Operation.escape_path(path)}/tune",
      opts,
      body: [
        :allowed_response_headers,
        :audit_non_hmac_request_keys,
        :audit_non_hmac_response_keys,
        :default_lease_ttl,
        :description,
        :listing_visibility,
        :max_lease_ttl,
        :options,
        :passthrough_request_headers,
        :plugin_version,
        :token_type,
        :user_lockout_config
      ]
    )
  end

  @doc ~S"""
  Delete a namespace.

  `DELETE /v1/sys/namespaces/{path}`

  Create, read, update and delete namespaces.

  ## Arguments

    * `path` — Path of the namespace.
  """
  @operation "namespaces-delete-namespaces-path"
  @spec namespaces_delete_namespaces_path(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def namespaces_delete_namespaces_path(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :delete,
      "sys/namespaces/#{ExBao.Operation.escape_path(path)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Delete a sealed namespace by wiping its physical storage.

  `DELETE /v1/sys/namespaces/{path}/delete-sealed`

  Delete a sealed namespace.

  ## Arguments

    * `path` — Path of the namespace.
  """
  @operation "namespaces-delete-namespaces-path-delete-sealed"
  @spec namespaces_delete_namespaces_path_delete_sealed(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def namespaces_delete_namespaces_path_delete_sealed(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :delete,
      "sys/namespaces/#{ExBao.Operation.escape_path(path)}/delete-sealed",
      opts,
      []
    )
  end

  @doc ~S"""
  Update a namespace's custom metadata.

  `PATCH /v1/sys/namespaces/{path}`

  Create, read, update and delete namespaces.

  ## Arguments

    * `path` — Path of the namespace.

  ## Options

    * `:custom_metadata` (map) — User provided key-value pairs.
    * `:pgp_keys` (array) — Specifies an array of PGP public keys used to encrypt the output unseal keys.
    * `:seal` (string) — User provided seal config.
  """
  @operation "namespaces-patch-namespaces-path"
  @spec namespaces_patch_namespaces_path(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def namespaces_patch_namespaces_path(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :patch,
      "sys/namespaces/#{ExBao.Operation.escape_path(path)}",
      opts,
      body: [:custom_metadata, :pgp_keys, :seal],
      content_type: "application/merge-patch+json"
    )
  end

  @doc ~S"""
  Retrieve a namespace.

  `GET /v1/sys/namespaces/{path}`

  Create, read, update and delete namespaces.

  ## Arguments

    * `path` — Path of the namespace.
  """
  @operation "namespaces-read-namespaces-path"
  @spec namespaces_read_namespaces_path(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def namespaces_read_namespaces_path(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/namespaces/#{ExBao.Operation.escape_path(path)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Check the seal status of an OpenBao namespace.

  `GET /v1/sys/namespaces/{path}/seal-status`

  Seal, unseal and delete sealable namespaces and check their seal status.

  ## Arguments

    * `path` — Path of the namespace.
  """
  @operation "namespaces-read-seal"
  @spec namespaces_read_seal(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def namespaces_read_seal(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/namespaces/#{ExBao.Operation.escape_path(path)}/seal-status",
      opts,
      []
    )
  end

  @doc ~S"""
  Scan (recursively list) namespaces.

  `GET /v1/sys/namespaces`

  List namespaces.
  """
  @operation "namespaces-scan-namespaces"
  @spec namespaces_scan_namespaces(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def namespaces_scan_namespaces(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/namespaces", opts,
      fixed_query: %{"list" => "true", "scan" => "true"}
    )
  end

  @doc ~S"""
  Seal a namespace.

  `POST /v1/sys/namespaces/{path}/seal`

  Seal, unseal and delete sealable namespaces and check their seal status.

  ## Arguments

    * `path` — Path of the namespace.
  """
  @operation "namespaces-seal"
  @spec namespaces_seal(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def namespaces_seal(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/namespaces/#{ExBao.Operation.escape_path(path)}/seal",
      opts,
      []
    )
  end

  @doc ~S"""
  Unseal a namespace.

  `POST /v1/sys/namespaces/{path}/unseal`

  Seal, unseal and delete sealable namespaces and check their seal status.

  ## Arguments

    * `path` — Path of the namespace.

  ## Options

    * `:key` (string) — Specifies a single namespace unseal key share.
    * `:reset` (boolean) — Specifies whether to reset an unseal process progress.
  """
  @operation "namespaces-unseal"
  @spec namespaces_unseal(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def namespaces_unseal(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/namespaces/#{ExBao.Operation.escape_path(path)}/unseal",
      opts,
      body: [:key, :reset]
    )
  end

  @doc ~S"""
  Lock a namespace.

  `POST /v1/sys/namespaces/api-lock/lock`

  ## Options

    * `:path` (string) — Path of the namespace.
  """
  @operation "namespaces-write-namespaces-api-lock-lock"
  @spec namespaces_write_namespaces_api_lock_lock(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def namespaces_write_namespaces_api_lock_lock(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/namespaces/api-lock/lock", opts, body: [:path])
  end

  @doc ~S"""
  Lock a namespace.

  `POST /v1/sys/namespaces/api-lock/lock/{path}`

  ## Arguments

    * `path` — Path of the namespace.
  """
  @operation "namespaces-write-namespaces-api-lock-lock-path"
  @spec namespaces_write_namespaces_api_lock_lock_path(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def namespaces_write_namespaces_api_lock_lock_path(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/namespaces/api-lock/lock/#{ExBao.Operation.escape_path(path)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Unlock a namespace.

  `POST /v1/sys/namespaces/api-lock/unlock`

  ## Options

    * `:path` (string) — Path of the namespace.
    * `:unlock_key` (string, required) — Unlock key required for unlocking the namespace
  """
  @operation "namespaces-write-namespaces-api-lock-unlock"
  @spec namespaces_write_namespaces_api_lock_unlock(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def namespaces_write_namespaces_api_lock_unlock(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/namespaces/api-lock/unlock", opts,
      body: [:path, :unlock_key],
      required: [:unlock_key]
    )
  end

  @doc ~S"""
  Unlock a namespace.

  `POST /v1/sys/namespaces/api-lock/unlock/{path}`

  ## Arguments

    * `path` — Path of the namespace.

  ## Options

    * `:unlock_key` (string, required) — Unlock key required for unlocking the namespace
  """
  @operation "namespaces-write-namespaces-api-lock-unlock-path"
  @spec namespaces_write_namespaces_api_lock_unlock_path(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def namespaces_write_namespaces_api_lock_unlock_path(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/namespaces/api-lock/unlock/#{ExBao.Operation.escape_path(path)}",
      opts,
      body: [:unlock_key],
      required: [:unlock_key]
    )
  end

  @doc ~S"""
  Create or update a namespace.

  `POST /v1/sys/namespaces/{path}`

  Create, read, update and delete namespaces.

  ## Arguments

    * `path` — Path of the namespace.

  ## Options

    * `:custom_metadata` (map) — User provided key-value pairs.
    * `:pgp_keys` (array) — Specifies an array of PGP public keys used to encrypt the output unseal keys.
    * `:seal` (string) — User provided seal config.
  """
  @operation "namespaces-write-namespaces-path"
  @spec namespaces_write_namespaces_path(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def namespaces_write_namespaces_path(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/namespaces/#{ExBao.Operation.escape_path(path)}",
      opts,
      body: [:custom_metadata, :pgp_keys, :seal]
    )
  end

  @doc ~S"""
  Lists all the plugins known to OpenBao

  `GET /v1/sys/plugins/catalog`
  """
  @operation "plugins-catalog-list-plugins"
  @spec plugins_catalog_list_plugins(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def plugins_catalog_list_plugins(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/plugins/catalog", opts, [])
  end

  @doc ~S"""
  List the plugins in the catalog.

  `GET /v1/sys/plugins/catalog/{type}` — needs `sudo`

  Configures the plugins known to OpenBao

  ## Arguments

    * `type` — The type of the plugin, may be auth, secret, or database
  """
  @operation "plugins-catalog-list-plugins-with-type"
  @spec plugins_catalog_list_plugins_with_type(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def plugins_catalog_list_plugins_with_type(server, type, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/plugins/catalog/#{ExBao.Operation.escape(type)}",
      opts,
      fixed_query: %{"list" => "true"}
    )
  end

  @doc ~S"""
  Return the configuration data for the plugin with the given name.

  `GET /v1/sys/plugins/catalog/{name}` — needs `sudo`

  Configures the plugins known to OpenBao

  ## Arguments

    * `name` — The name of the plugin
  """
  @operation "plugins-catalog-read-plugin-configuration"
  @spec plugins_catalog_read_plugin_configuration(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def plugins_catalog_read_plugin_configuration(server, name, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/plugins/catalog/#{ExBao.Operation.escape(name)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Return the configuration data for the plugin with the given name.

  `GET /v1/sys/plugins/catalog/{type}/{name}` — needs `sudo`

  Configures the plugins known to OpenBao

  ## Arguments

    * `type` — The type of the plugin, may be auth, secret, or database
    * `name` — The name of the plugin
  """
  @operation "plugins-catalog-read-plugin-configuration-with-type"
  @spec plugins_catalog_read_plugin_configuration_with_type(
          ExBao.server(),
          String.t(),
          String.t(),
          keyword()
        ) ::
          ExBao.Operation.result()
  def plugins_catalog_read_plugin_configuration_with_type(server, type, name, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/plugins/catalog/#{ExBao.Operation.escape(type)}/#{ExBao.Operation.escape(name)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Register a new plugin, or updates an existing one with the supplied name.

  `POST /v1/sys/plugins/catalog/{name}` — needs `sudo`

  Configures the plugins known to OpenBao

  ## Arguments

    * `name` — The name of the plugin

  ## Options

    * `:args` (array) — The args passed to plugin command.
    * `:command` (string) — The command used to start the plugin. The executable defined in this command must exist in OpenBao's plugin directory.
    * `:env` (array) — The environment variables passed to plugin command. Each entry is of the form "key=value".
    * `:oci` (boolean) —
    * `:sha256` (string) — The SHA256 sum of the executable used in the command field. This should be HEX encoded.
    * `:type` (string) — The type of the plugin, may be auth, secret, or database
    * `:version` (string) — The semantic version of the plugin to use.
  """
  @operation "plugins-catalog-register-plugin"
  @spec plugins_catalog_register_plugin(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def plugins_catalog_register_plugin(server, name, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/plugins/catalog/#{ExBao.Operation.escape(name)}",
      opts,
      body: [:args, :command, :env, :oci, :sha256, :type, :version]
    )
  end

  @doc ~S"""
  Register a new plugin, or updates an existing one with the supplied name.

  `POST /v1/sys/plugins/catalog/{type}/{name}` — needs `sudo`

  Configures the plugins known to OpenBao

  ## Arguments

    * `type` — The type of the plugin, may be auth, secret, or database
    * `name` — The name of the plugin

  ## Options

    * `:args` (array) — The args passed to plugin command.
    * `:command` (string) — The command used to start the plugin. The executable defined in this command must exist in OpenBao's plugin directory.
    * `:env` (array) — The environment variables passed to plugin command. Each entry is of the form "key=value".
    * `:oci` (boolean) —
    * `:sha256` (string) — The SHA256 sum of the executable used in the command field. This should be HEX encoded.
    * `:version` (string) — The semantic version of the plugin to use.
  """
  @operation "plugins-catalog-register-plugin-with-type"
  @spec plugins_catalog_register_plugin_with_type(
          ExBao.server(),
          String.t(),
          String.t(),
          keyword()
        ) ::
          ExBao.Operation.result()
  def plugins_catalog_register_plugin_with_type(server, type, name, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/plugins/catalog/#{ExBao.Operation.escape(type)}/#{ExBao.Operation.escape(name)}",
      opts,
      body: [:args, :command, :env, :oci, :sha256, :version]
    )
  end

  @doc ~S"""
  Remove the plugin with the given name.

  `DELETE /v1/sys/plugins/catalog/{name}` — needs `sudo`

  Configures the plugins known to OpenBao

  ## Arguments

    * `name` — The name of the plugin
  """
  @operation "plugins-catalog-remove-plugin"
  @spec plugins_catalog_remove_plugin(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def plugins_catalog_remove_plugin(server, name, opts \\ []) do
    ExBao.Operation.call(
      server,
      :delete,
      "sys/plugins/catalog/#{ExBao.Operation.escape(name)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Remove the plugin with the given name.

  `DELETE /v1/sys/plugins/catalog/{type}/{name}` — needs `sudo`

  Configures the plugins known to OpenBao

  ## Arguments

    * `type` — The type of the plugin, may be auth, secret, or database
    * `name` — The name of the plugin
  """
  @operation "plugins-catalog-remove-plugin-with-type"
  @spec plugins_catalog_remove_plugin_with_type(ExBao.server(), String.t(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def plugins_catalog_remove_plugin_with_type(server, type, name, opts \\ []) do
    ExBao.Operation.call(
      server,
      :delete,
      "sys/plugins/catalog/#{ExBao.Operation.escape(type)}/#{ExBao.Operation.escape(name)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Reload mounted plugin backends.

  `POST /v1/sys/plugins/reload/backend`

  Reload mounts that use a particular backend plugin.

  ## Options

    * `:mounts` (array) — The mount paths of the plugin backends to reload.
    * `:plugin` (string) — The name of the plugin to reload, as registered in the plugin catalog.
    * `:scope` (string) —
  """
  @operation "plugins-reload-backends"
  @spec plugins_reload_backends(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def plugins_reload_backends(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/plugins/reload/backend", opts,
      body: [:mounts, :plugin, :scope]
    )
  end

  @doc ~S"""
  Delete the ACL policy with the given name.

  `DELETE /v1/sys/policies/acl/{name}`

  Read, Modify, Patch, or Delete an access control policy.

  ## Arguments

    * `name` — The name of the policy. Example: "ops"
  """
  @operation "policies-delete-acl-policy"
  @spec policies_delete_acl_policy(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def policies_delete_acl_policy(server, name, opts \\ []) do
    ExBao.Operation.call(
      server,
      :delete,
      "sys/policies/acl/#{ExBao.Operation.escape(name)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Delete the policy with the given name.

  `DELETE /v1/sys/policy/{name}`

  Read, Modify, Patch, or Delete an access control policy.

  ## Arguments

    * `name` — The name of the policy. Example: "ops"
  """
  @operation "policies-delete-acl-policy2"
  @spec policies_delete_acl_policy2(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def policies_delete_acl_policy2(server, name, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/policy/#{ExBao.Operation.escape(name)}", opts, [])
  end

  @doc ~S"""
  Delete a password policy.

  `DELETE /v1/sys/policies/password/{name}`

  Read, Modify, or Delete a password policy.

  ## Arguments

    * `name` — The name of the password policy.
  """
  @operation "policies-delete-password-policy"
  @spec policies_delete_password_policy(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def policies_delete_password_policy(server, name, opts \\ []) do
    ExBao.Operation.call(
      server,
      :delete,
      "sys/policies/password/#{ExBao.Operation.escape(name)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Generate a password from an existing password policy.

  `GET /v1/sys/policies/password/{name}/generate`

  ## Arguments

    * `name` — The name of the password policy.
  """
  @operation "policies-generate-password-from-password-policy"
  @spec policies_generate_password_from_password_policy(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def policies_generate_password_from_password_policy(server, name, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/policies/password/#{ExBao.Operation.escape(name)}/generate",
      opts,
      []
    )
  end

  @doc ~S"""
  List the configured access control policies.

  `GET /v1/sys/policy`

  ## Options

    * `:list` (string) — Return a list if `true`
  """
  @operation "policies-list"
  @spec policies_list(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def policies_list(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/policy", opts, query: [:list])
  end

  @doc ~S"""
  List the configured access control policies.

  `GET /v1/sys/policies/acl`
  """
  @operation "policies-list-acl-policies"
  @spec policies_list_acl_policies(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def policies_list_acl_policies(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/policies/acl", opts, fixed_query: %{"list" => "true"})
  end

  @doc ~S"""
  List the existing password policies.

  `GET /v1/sys/policies/password`
  """
  @operation "policies-list-password-policies"
  @spec policies_list_password_policies(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def policies_list_password_policies(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/policies/password", opts,
      fixed_query: %{"list" => "true"}
    )
  end

  @doc ~S"""
  Patches an existing ACL policy.

  `PATCH /v1/sys/policies/acl/{name}`

  Read, Modify, Patch, or Delete an access control policy.

  ## Arguments

    * `name` — The name of the policy. Example: "ops"

  ## Options

    * `:allow_slashes_in_identity_templates` (boolean) —  Defaults to `false`.
    * `:allow_wildcards_in_identity_templates` (boolean) —  Defaults to `false`.
    * `:cas` (integer) — The rules of the policy.
    * `:cas_required` (boolean) — The rules of the policy.
    * `:expiration` (date-time) — The rules of the policy.
    * `:policy` (string) — The rules of the policy.
    * `:ttl` (seconds) — The rules of the policy.
  """
  @operation "policies-patch-acl-policy"
  @spec policies_patch_acl_policy(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def policies_patch_acl_policy(server, name, opts \\ []) do
    ExBao.Operation.call(server, :patch, "sys/policies/acl/#{ExBao.Operation.escape(name)}", opts,
      body: [
        :allow_slashes_in_identity_templates,
        :allow_wildcards_in_identity_templates,
        :cas,
        :cas_required,
        :expiration,
        :policy,
        :ttl
      ],
      content_type: "application/merge-patch+json"
    )
  end

  @doc ~S"""
  Retrieve information about the named ACL policy.

  `GET /v1/sys/policies/acl/{name}`

  Read, Modify, Patch, or Delete an access control policy.

  ## Arguments

    * `name` — The name of the policy. Example: "ops"

  ## Options

    * `:list` (string) — Return a list if `true`
  """
  @operation "policies-read-acl-policy"
  @spec policies_read_acl_policy(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def policies_read_acl_policy(server, name, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/policies/acl/#{ExBao.Operation.escape(name)}", opts,
      query: [:list]
    )
  end

  @doc ~S"""
  Retrieve the policy body for the named policy.

  `GET /v1/sys/policy/{name}`

  Read, Modify, Patch, or Delete an access control policy.

  ## Arguments

    * `name` — The name of the policy. Example: "ops"

  ## Options

    * `:list` (string) — Return a list if `true`
  """
  @operation "policies-read-acl-policy2"
  @spec policies_read_acl_policy2(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def policies_read_acl_policy2(server, name, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/policy/#{ExBao.Operation.escape(name)}", opts,
      query: [:list]
    )
  end

  @doc ~S"""
  Retrieve an existing password policy.

  `GET /v1/sys/policies/password/{name}`

  Read, Modify, or Delete a password policy.

  ## Arguments

    * `name` — The name of the password policy.
  """
  @operation "policies-read-password-policy"
  @spec policies_read_password_policy(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def policies_read_password_policy(server, name, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/policies/password/#{ExBao.Operation.escape(name)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Add a new or update an existing ACL policy.

  `POST /v1/sys/policies/acl/{name}`

  Read, Modify, Patch, or Delete an access control policy.

  ## Arguments

    * `name` — The name of the policy. Example: "ops"

  ## Options

    * `:allow_slashes_in_identity_templates` (boolean) —  Defaults to `false`.
    * `:allow_wildcards_in_identity_templates` (boolean) —  Defaults to `false`.
    * `:cas` (integer) — The rules of the policy.
    * `:cas_required` (boolean) — The rules of the policy.
    * `:expiration` (date-time) — The rules of the policy.
    * `:policy` (string) — The rules of the policy.
    * `:ttl` (seconds) — The rules of the policy.
  """
  @operation "policies-write-acl-policy"
  @spec policies_write_acl_policy(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def policies_write_acl_policy(server, name, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/policies/acl/#{ExBao.Operation.escape(name)}", opts,
      body: [
        :allow_slashes_in_identity_templates,
        :allow_wildcards_in_identity_templates,
        :cas,
        :cas_required,
        :expiration,
        :policy,
        :ttl
      ]
    )
  end

  @doc ~S"""
  Add a new or update an existing policy.

  `POST /v1/sys/policy/{name}`

  Read, Modify, Patch, or Delete an access control policy.

  ## Arguments

    * `name` — The name of the policy. Example: "ops"

  ## Options

    * `:allow_slashes_in_identity_templates` (boolean) —  Defaults to `false`.
    * `:allow_wildcards_in_identity_templates` (boolean) —  Defaults to `false`.
    * `:cas` (integer) — The rules of the policy.
    * `:cas_required` (boolean) — The rules of the policy.
    * `:expiration` (date-time) — The rules of the policy.
    * `:policy` (string) — The rules of the policy.
    * `:rules` (string, deprecated) — The rules of the policy.
    * `:ttl` (seconds) — The rules of the policy.
  """
  @operation "policies-write-acl-policy2"
  @spec policies_write_acl_policy2(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def policies_write_acl_policy2(server, name, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/policy/#{ExBao.Operation.escape(name)}", opts,
      body: [
        :allow_slashes_in_identity_templates,
        :allow_wildcards_in_identity_templates,
        :cas,
        :cas_required,
        :expiration,
        :policy,
        :rules,
        :ttl
      ]
    )
  end

  @doc ~S"""
  Add a new or update an existing password policy.

  `POST /v1/sys/policies/password/{name}`

  Read, Modify, or Delete a password policy.

  ## Arguments

    * `name` — The name of the password policy.

  ## Options

    * `:policy` (string) — The password policy
  """
  @operation "policies-write-password-policy"
  @spec policies_write_password_policy(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def policies_write_password_policy(server, name, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/policies/password/#{ExBao.Operation.escape(name)}",
      opts,
      body: [:policy]
    )
  end

  @doc ~S"""
  Returns stack traces that led to blocking on synchronization primitives

  `GET /v1/sys/pprof/block`
  """
  @operation "pprof-blocking"
  @spec pprof_blocking(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def pprof_blocking(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/pprof/block", opts, [])
  end

  @doc ~S"""
  Returns the running program's command line.

  `GET /v1/sys/pprof/cmdline`
  """
  @operation "pprof-command-line"
  @spec pprof_command_line(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def pprof_command_line(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/pprof/cmdline", opts, [])
  end

  @doc ~S"""
  Returns a pprof-formatted cpu profile payload.

  `GET /v1/sys/pprof/profile`
  """
  @operation "pprof-cpu-profile"
  @spec pprof_cpu_profile(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def pprof_cpu_profile(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/pprof/profile", opts, [])
  end

  @doc ~S"""
  Returns the execution trace in binary form.

  `GET /v1/sys/pprof/trace`
  """
  @operation "pprof-execution-trace"
  @spec pprof_execution_trace(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def pprof_execution_trace(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/pprof/trace", opts, [])
  end

  @doc ~S"""
  Returns stack traces of all current goroutines.

  `GET /v1/sys/pprof/goroutine`
  """
  @operation "pprof-goroutines"
  @spec pprof_goroutines(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def pprof_goroutines(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/pprof/goroutine", opts, [])
  end

  @doc ~S"""
  Returns an HTML page listing the available profiles.

  `GET /v1/sys/pprof`
  """
  @operation "pprof-index"
  @spec pprof_index(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def pprof_index(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/pprof", opts, [])
  end

  @doc ~S"""
  Returns a sampling of all past memory allocations.

  `GET /v1/sys/pprof/allocs`
  """
  @operation "pprof-memory-allocations"
  @spec pprof_memory_allocations(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def pprof_memory_allocations(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/pprof/allocs", opts, [])
  end

  @doc ~S"""
  Returns a sampling of memory allocations of live object.

  `GET /v1/sys/pprof/heap`
  """
  @operation "pprof-memory-allocations-live"
  @spec pprof_memory_allocations_live(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def pprof_memory_allocations_live(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/pprof/heap", opts, [])
  end

  @doc ~S"""
  Returns stack traces of holders of contended mutexes

  `GET /v1/sys/pprof/mutex`
  """
  @operation "pprof-mutexes"
  @spec pprof_mutexes(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def pprof_mutexes(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/pprof/mutex", opts, [])
  end

  @doc ~S"""
  Returns the program counters listed in the request.

  `GET /v1/sys/pprof/symbol`
  """
  @operation "pprof-symbols"
  @spec pprof_symbols(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def pprof_symbols(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/pprof/symbol", opts, [])
  end

  @doc ~S"""
  Returns stack traces that led to the creation of new OS threads

  `GET /v1/sys/pprof/threadcreate`
  """
  @operation "pprof-thread-creations"
  @spec pprof_thread_creations(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def pprof_thread_creations(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/pprof/threadcreate", opts, [])
  end

  @doc ~S"""
  Fetches the capabilities of the token associated with the given token, on the given path.

  `POST /v1/sys/capabilities-accessor`

  ## Options

    * `:accessor` (string) — Accessor of the token for which capabilities are being queried.
    * `:path` (array, deprecated) — Use 'paths' instead.
    * `:paths` (array) — Paths on which capabilities are being queried.
  """
  @operation "query-token-accessor-capabilities"
  @spec query_token_accessor_capabilities(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def query_token_accessor_capabilities(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/capabilities-accessor", opts,
      body: [:accessor, :path, :paths]
    )
  end

  @doc ~S"""
  Fetches the capabilities of the given token on the given path.

  `POST /v1/sys/capabilities`

  ## Options

    * `:path` (array, deprecated) — Use 'paths' instead.
    * `:paths` (array) — Paths on which capabilities are being queried.
    * `:token` (string) — Token for which capabilities are being queried.
  """
  @operation "query-token-capabilities"
  @spec query_token_capabilities(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def query_token_capabilities(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/capabilities", opts, body: [:path, :paths, :token])
  end

  @doc ~S"""
  Fetches the capabilities of the given token on the given path.

  `POST /v1/sys/capabilities-self`

  ## Options

    * `:path` (array, deprecated) — Use 'paths' instead.
    * `:paths` (array) — Paths on which capabilities are being queried.
    * `:token` (string) — Token for which capabilities are being queried.
  """
  @operation "query-token-self-capabilities"
  @spec query_token_self_capabilities(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def query_token_self_capabilities(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/capabilities-self", opts,
      body: [:path, :paths, :token]
    )
  end

  @doc ~S"""
  Create, update and read the quota configuration.

  `POST /v1/sys/quotas/config`

  ## Options

    * `:enable_rate_limit_audit_logging` (boolean) — If set, starts audit logging of requests that get rejected due to rate limit quota rule violations.
    * `:enable_rate_limit_response_headers` (boolean) — If set, additional rate limit quota HTTP headers will be added to responses.
    * `:rate_limit_exempt_paths` (array) — Specifies the list of exempt paths from all rate limit quotas. If empty no paths will be exempt.
  """
  @operation "rate-limit-quotas-configure"
  @spec rate_limit_quotas_configure(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rate_limit_quotas_configure(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/quotas/config", opts,
      body: [
        :enable_rate_limit_audit_logging,
        :enable_rate_limit_response_headers,
        :rate_limit_exempt_paths
      ]
    )
  end

  @doc ~S"""
  Get, create or update rate limit resource quota for an optional namespace or mount.

  `DELETE /v1/sys/quotas/rate-limit/{name}`

  ## Arguments

    * `name` — Name of the quota rule.
  """
  @operation "rate-limit-quotas-delete"
  @spec rate_limit_quotas_delete(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def rate_limit_quotas_delete(server, name, opts \\ []) do
    ExBao.Operation.call(
      server,
      :delete,
      "sys/quotas/rate-limit/#{ExBao.Operation.escape(name)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Lists the names of all the rate limit quotas.

  `GET /v1/sys/quotas/rate-limit`
  """
  @operation "rate-limit-quotas-list"
  @spec rate_limit_quotas_list(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rate_limit_quotas_list(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/quotas/rate-limit", opts,
      fixed_query: %{"list" => "true"}
    )
  end

  @doc ~S"""
  Get, create or update rate limit resource quota for an optional namespace or mount.

  `GET /v1/sys/quotas/rate-limit/{name}`

  ## Arguments

    * `name` — Name of the quota rule.
  """
  @operation "rate-limit-quotas-read"
  @spec rate_limit_quotas_read(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def rate_limit_quotas_read(server, name, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/quotas/rate-limit/#{ExBao.Operation.escape(name)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Create, update and read the quota configuration.

  `GET /v1/sys/quotas/config`
  """
  @operation "rate-limit-quotas-read-configuration"
  @spec rate_limit_quotas_read_configuration(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rate_limit_quotas_read_configuration(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/quotas/config", opts, [])
  end

  @doc ~S"""
  Get, create or update rate limit resource quota for an optional namespace or mount.

  `POST /v1/sys/quotas/rate-limit/{name}`

  ## Arguments

    * `name` — Name of the quota rule.

  ## Options

    * `:block_interval` (seconds) — If set, when a client reaches a rate limit threshold, the client will be prohibited from any further requests until after the 'block_interval' has elapsed.
    * `:inheritable` (boolean) — If set to true, child namespaces will use this quota, unless another more specific quota exists. Can only be set on namespace quotas. A quota on the root namespace will by default be inheritable.
    * `:interval` (seconds) — The duration to enforce rate limiting for (default '1s').
    * `:path` (string) — Path of the mount or namespace to apply the quota. A blank path configures a global quota. For example namespace1/ adds a quota to a full namespace, namespace1/auth/userpass adds a quota to userpass in namespace1.
    * `:rate` (float) — The maximum number of requests in a given interval to be allowed by the quota rule. The 'rate' must be positive.
    * `:role` (string) — Login role to apply this quota to. Note that when set, path must be configured to a valid auth method with a concept of roles.
    * `:type` (string) — Type of the quota rule.
  """
  @operation "rate-limit-quotas-write"
  @spec rate_limit_quotas_write(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def rate_limit_quotas_write(server, name, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/quotas/rate-limit/#{ExBao.Operation.escape(name)}",
      opts,
      body: [:block_interval, :inheritable, :interval, :path, :rate, :role, :type]
    )
  end

  @doc ~S"""
  Delete the key with given path.

  `DELETE /v1/sys/raw` — needs `sudo`

  Write, Read, and Delete data directly in the Storage backend.
  """
  @operation "raw-delete"
  @spec raw_delete(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def raw_delete(server, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/raw", opts, [])
  end

  @doc ~S"""
  Delete the key with given path.

  `DELETE /v1/sys/raw/{path}` — needs `sudo`

  Write, Read, and Delete data directly in the Storage backend.

  ## Arguments

    * `path` — 
  """
  @operation "raw-delete-path"
  @spec raw_delete_path(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def raw_delete_path(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :delete,
      "sys/raw/#{ExBao.Operation.escape_path(path)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Read the value of the key at the given path.

  `GET /v1/sys/raw` — needs `sudo`

  Write, Read, and Delete data directly in the Storage backend.

  ## Options

    * `:list` (string) — Return a list if `true`
  """
  @operation "raw-read"
  @spec raw_read(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def raw_read(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/raw", opts, query: [:list])
  end

  @doc ~S"""
  Read the value of the key at the given path.

  `GET /v1/sys/raw/{path}` — needs `sudo`

  Write, Read, and Delete data directly in the Storage backend.

  ## Arguments

    * `path` — 

  ## Options

    * `:list` (string) — Return a list if `true`
  """
  @operation "raw-read-path"
  @spec raw_read_path(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def raw_read_path(server, path, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/raw/#{ExBao.Operation.escape_path(path)}", opts,
      query: [:list]
    )
  end

  @doc ~S"""
  Update the value of the key at the given path.

  `POST /v1/sys/raw` — needs `sudo`

  Write, Read, and Delete data directly in the Storage backend.

  ## Options

    * `:after` (string) — Optional entry to list begin listing after, not required to exist. Only used in list operations.
    * `:compressed` (boolean) —
    * `:compression_type` (string) —
    * `:encoding` (string) —
    * `:limit` (integer) — Optional number of entries to return; defaults to all entries. Only used in list operations.
    * `:path` (string) —
    * `:value` (string) —
  """
  @operation "raw-write"
  @spec raw_write(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def raw_write(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/raw", opts,
      body: [:after, :compressed, :compression_type, :encoding, :limit, :path, :value]
    )
  end

  @doc ~S"""
  Update the value of the key at the given path.

  `POST /v1/sys/raw/{path}` — needs `sudo`

  Write, Read, and Delete data directly in the Storage backend.

  ## Arguments

    * `path` — 

  ## Options

    * `:after` (string) — Optional entry to list begin listing after, not required to exist. Only used in list operations.
    * `:compressed` (boolean) —
    * `:compression_type` (string) —
    * `:encoding` (string) —
    * `:limit` (integer) — Optional number of entries to return; defaults to all entries. Only used in list operations.
    * `:value` (string) —
  """
  @operation "raw-write-path"
  @spec raw_write_path(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def raw_write_path(server, path, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/raw/#{ExBao.Operation.escape_path(path)}", opts,
      body: [:after, :compressed, :compression_type, :encoding, :limit, :value]
    )
  end

  @doc ~S"""
  Get the automatic key rotation config.

  `GET /v1/sys/rotate/config`
  """
  @operation "read-encryption-key-config"
  @spec read_encryption_key_config(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def read_encryption_key_config(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/rotate/config", opts, [])
  end

  @doc ~S"""
  Returns the health status of OpenBao.

  `GET /v1/sys/health` — needs no token

  Checks the health status of OpenBao.
  """
  @operation "read-health-status"
  @spec read_health_status(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def read_health_status(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/health", opts, [])
  end

  @doc ~S"""
  Returns the initialization status of OpenBao.

  `GET /v1/sys/init` — needs no token

  Initializes or returns the initialization status of OpenBao.
  """
  @operation "read-initialization-status"
  @spec read_initialization_status(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def read_initialization_status(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/init", opts, [])
  end

  @doc ~S"""
  Get the automatic key rotation config.

  `GET /v1/sys/rotate/keyring/config`
  """
  @operation "read-rotate-keyring-config"
  @spec read_rotate_keyring_config(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def read_rotate_keyring_config(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/rotate/keyring/config", opts, [])
  end

  @doc ~S"""
  Return a sanitized version of the OpenBao server configuration.

  `GET /v1/sys/config/state/sanitized`
  """
  @operation "read-sanitized-configuration-state"
  @spec read_sanitized_configuration_state(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def read_sanitized_configuration_state(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/config/state/sanitized", opts, [])
  end

  @doc ~S"""
  Look up wrapping properties for the given token.

  `POST /v1/sys/wrapping/lookup` — needs no token

  Looks up the properties of a response-wrapped token.

  ## Options

    * `:token` (string) —
  """
  @operation "read-wrapping-properties"
  @spec read_wrapping_properties(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def read_wrapping_properties(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/wrapping/lookup", opts, body: [:token])
  end

  @doc ~S"""
  Look up wrapping properties for the requester's token.

  `GET /v1/sys/wrapping/lookup` — needs no token

  Looks up the properties of a response-wrapped token.
  """
  @operation "read-wrapping-properties2"
  @spec read_wrapping_properties2(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def read_wrapping_properties2(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/wrapping/lookup", opts, [])
  end

  @doc ~S"""
  Cancels any in-progress rekey.

  `DELETE /v1/sys/rekey/init` — needs no token
  """
  @operation "rekey-attempt-cancel"
  @spec rekey_attempt_cancel(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rekey_attempt_cancel(server, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/rekey/init", opts, [])
  end

  @doc ~S"""
  Initializes a new rekey attempt.

  `POST /v1/sys/rekey/init` — needs no token

  ## Options

    * `:backup` (boolean) — Specifies if using PGP-encrypted keys, whether OpenBao should also store a plaintext backup of the PGP-encrypted keys.
    * `:pgp_keys` (array) — Specifies an array of PGP public keys used to encrypt the output unseal keys. Ordering is preserved. The keys must be base64-encoded from their original binary representation. The size of this array must be the same as secret_shares.
    * `:require_verification` (boolean) — Turns on verification functionality
    * `:secret_shares` (integer) — Specifies the number of shares to split the unseal key into.
    * `:secret_threshold` (integer) — Specifies the number of shares required to reconstruct the unseal key. This must be less than or equal secret_shares. If using OpenBao HSM with auto-unsealing, this value must be the same as secret_shares.
  """
  @operation "rekey-attempt-initialize"
  @spec rekey_attempt_initialize(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rekey_attempt_initialize(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/rekey/init", opts,
      body: [:backup, :pgp_keys, :require_verification, :secret_shares, :secret_threshold]
    )
  end

  @doc ~S"""
  Reads the configuration and progress of the current rekey attempt.

  `GET /v1/sys/rekey/init` — needs no token
  """
  @operation "rekey-attempt-read-progress"
  @spec rekey_attempt_read_progress(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rekey_attempt_read_progress(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/rekey/init", opts, [])
  end

  @doc ~S"""
  Enter a single unseal key share to progress the rekey of the OpenBao.

  `POST /v1/sys/rekey/update` — needs no token

  ## Options

    * `:key` (string) — Specifies a single unseal key share.
    * `:nonce` (string) — Specifies the nonce of the rekey attempt.
  """
  @operation "rekey-attempt-update"
  @spec rekey_attempt_update(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rekey_attempt_update(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/rekey/update", opts, body: [:key, :nonce])
  end

  @doc ~S"""
  Delete the backup copy of PGP-encrypted unseal keys.

  `DELETE /v1/sys/rekey/backup`

  Allows fetching or deleting the backup of the rotated unseal keys.
  """
  @operation "rekey-delete-backup-key"
  @spec rekey_delete_backup_key(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rekey_delete_backup_key(server, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/rekey/backup", opts, [])
  end

  @doc ~S"""
  Allows fetching or deleting the backup of the rotated unseal keys.

  `DELETE /v1/sys/rekey/recovery-key-backup`
  """
  @operation "rekey-delete-backup-recovery-key"
  @spec rekey_delete_backup_recovery_key(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rekey_delete_backup_recovery_key(server, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/rekey/recovery-key-backup", opts, [])
  end

  @doc ~S"""
  Return the backup copy of PGP-encrypted unseal keys.

  `GET /v1/sys/rekey/backup`

  Allows fetching or deleting the backup of the rotated unseal keys.
  """
  @operation "rekey-read-backup-key"
  @spec rekey_read_backup_key(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rekey_read_backup_key(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/rekey/backup", opts, [])
  end

  @doc ~S"""
  Allows fetching or deleting the backup of the rotated unseal keys.

  `GET /v1/sys/rekey/recovery-key-backup`
  """
  @operation "rekey-read-backup-recovery-key"
  @spec rekey_read_backup_recovery_key(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rekey_read_backup_recovery_key(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/rekey/recovery-key-backup", opts, [])
  end

  @doc ~S"""
  Cancel any in-progress rekey verification operation.

  `DELETE /v1/sys/rekey/verify` — needs no token
  """
  @operation "rekey-verification-cancel"
  @spec rekey_verification_cancel(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rekey_verification_cancel(server, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/rekey/verify", opts, [])
  end

  @doc ~S"""
  Read the configuration and progress of the current rekey verification attempt.

  `GET /v1/sys/rekey/verify` — needs no token
  """
  @operation "rekey-verification-read-progress"
  @spec rekey_verification_read_progress(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rekey_verification_read_progress(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/rekey/verify", opts, [])
  end

  @doc ~S"""
  Enter a single new key share to progress the rekey verification operation.

  `POST /v1/sys/rekey/verify` — needs no token

  ## Options

    * `:key` (string) — Specifies a single unseal share key from the new set of shares.
    * `:nonce` (string) — Specifies the nonce of the rekey verification operation.
  """
  @operation "rekey-verification-update"
  @spec rekey_verification_update(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rekey_verification_update(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/rekey/verify", opts, body: [:key, :nonce])
  end

  @doc ~S"""
  Reload the given subsystem

  `POST /v1/sys/config/reload/{subsystem}`

  ## Arguments

    * `subsystem` — 
  """
  @operation "reload-subsystem"
  @spec reload_subsystem(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def reload_subsystem(server, subsystem, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/config/reload/#{ExBao.Operation.escape(subsystem)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Initiate a mount migration

  `POST /v1/sys/remount` — needs `sudo`

  Move the mount point of an already-mounted backend, within or across namespaces

  ## Options

    * `:from` (string) — The previous mount point.
    * `:to` (string) — The new mount point.
  """
  @operation "remount"
  @spec remount(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def remount(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/remount", opts, body: [:from, :to])
  end

  @doc ~S"""
  Check status of a mount migration

  `GET /v1/sys/remount/status/{migration_id}`

  Check the status of a mount move operation

  ## Arguments

    * `migration_id` — The ID of the migration operation
  """
  @operation "remount-status"
  @spec remount_status(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def remount_status(server, migration_id, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/remount/status/#{ExBao.Operation.escape(migration_id)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Rotates a response-wrapped token.

  `POST /v1/sys/wrapping/rewrap`

  ## Options

    * `:token` (string) —
  """
  @operation "rewrap"
  @spec rewrap(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rewrap(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/wrapping/rewrap", opts, body: [:token])
  end

  @doc ~S"""
  Perform a root key rotation without requiring key shares to be provided.

  `POST /v1/sys/rotate/root` — needs `sudo`
  """
  @operation "root-key-rotate"
  @spec root_key_rotate(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def root_key_rotate(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/rotate/root", opts, [])
  end

  @doc ~S"""
  Cancels any in-progress root generation attempt.

  `DELETE /v1/sys/generate-root/attempt` — needs no token

  Reads status, initializes, or cancels a root token generation process.
  """
  @operation "root-token-generation-cancel"
  @spec root_token_generation_cancel(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def root_token_generation_cancel(server, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/generate-root/attempt", opts, [])
  end

  @doc ~S"""
  Cancels any in-progress root generation attempt.

  `DELETE /v1/sys/generate-root`

  Reads status, initializes, or cancels a root token generation process.
  """
  @operation "root-token-generation-cancel-2"
  @spec root_token_generation_cancel_2(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def root_token_generation_cancel_2(server, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/generate-root", opts, [])
  end

  @doc ~S"""
  Initializes a new root generation attempt.

  `POST /v1/sys/generate-root/attempt` — needs no token

  Reads status, initializes, or cancels a root token generation process.

  ## Options

    * `:pgp_key` (string) — Specifies a base64-encoded PGP public key.
  """
  @operation "root-token-generation-initialize"
  @spec root_token_generation_initialize(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def root_token_generation_initialize(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/generate-root/attempt", opts, body: [:pgp_key])
  end

  @doc ~S"""
  Initializes a new root generation attempt.

  `POST /v1/sys/generate-root`

  Reads status, initializes, or cancels a root token generation process.

  ## Options

    * `:pgp_key` (string) — Specifies a base64-encoded PGP public key.
  """
  @operation "root-token-generation-initialize-2"
  @spec root_token_generation_initialize_2(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def root_token_generation_initialize_2(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/generate-root", opts, body: [:pgp_key])
  end

  @doc ~S"""
  Read the configuration and progress of the current root generation attempt.

  `GET /v1/sys/generate-root/attempt` — needs no token

  Reads status, initializes, or cancels a root token generation process.
  """
  @operation "root-token-generation-read-progress"
  @spec root_token_generation_read_progress(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def root_token_generation_read_progress(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/generate-root/attempt", opts, [])
  end

  @doc ~S"""
  Read the configuration and progress of the current root generation attempt.

  `GET /v1/sys/generate-root`

  Reads status, initializes, or cancels a root token generation process.
  """
  @operation "root-token-generation-read-progress2"
  @spec root_token_generation_read_progress2(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def root_token_generation_read_progress2(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/generate-root", opts, [])
  end

  @doc ~S"""
  Enter a single unseal key share to progress the root generation attempt.

  `POST /v1/sys/generate-root/update` — needs no token

  Reads status, initializes, or cancels a root token generation process.

  ## Options

    * `:key` (string) — Specifies a single unseal key share.
    * `:nonce` (string) — Specifies the nonce of the attempt.
  """
  @operation "root-token-generation-update"
  @spec root_token_generation_update(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def root_token_generation_update(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/generate-root/update", opts, body: [:key, :nonce])
  end

  @doc ~S"""
  Cancels any in-progress rotate root operation.

  `DELETE /v1/sys/rotate/root/init`

  Initialize, read status or cancel the process of the rotation of the root or recovery key.
  """
  @operation "rotate-attempt-cancel"
  @spec rotate_attempt_cancel(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_attempt_cancel(server, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/rotate/root/init", opts, [])
  end

  @doc ~S"""
  Cancels any in-progress rotate root operation.

  `DELETE /v1/sys/rotate/recovery/init`

  Initialize, read status or cancel the process of the rotation of the root or recovery key.
  """
  @operation "rotate-attempt-cancel-rotate-recovery-init"
  @spec rotate_attempt_cancel_rotate_recovery_init(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_attempt_cancel_rotate_recovery_init(server, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/rotate/recovery/init", opts, [])
  end

  @doc ~S"""
  Initializes a new root rotate attempt.

  `POST /v1/sys/rotate/root/init`

  Initialize, read status or cancel the process of the rotation of the root or recovery key.

  ## Options

    * `:backup` (boolean) — Specifies if using PGP-encrypted keys, whether OpenBao should also store a plaintext backup of the said keys.
    * `:pgp_keys` (array) — Specifies an array of PGP public keys used to encrypt the output unseal keys.
    * `:require_verification` (boolean) — Enables verification which after successful authorization with the current unseal keys, ensures the new unseal keys are returned but the root key is not actually rotated.
    * `:secret_shares` (integer, required) — Specifies the number of shares to split the root key into.
    * `:secret_threshold` (integer, required) — Specifies the number of shares required to reconstruct the root key.
  """
  @operation "rotate-attempt-initialize"
  @spec rotate_attempt_initialize(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_attempt_initialize(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/rotate/root/init", opts,
      body: [:backup, :pgp_keys, :require_verification, :secret_shares, :secret_threshold],
      required: [:secret_shares, :secret_threshold]
    )
  end

  @doc ~S"""
  Initializes a new root rotate attempt.

  `POST /v1/sys/rotate/recovery/init`

  Initialize, read status or cancel the process of the rotation of the root or recovery key.

  ## Options

    * `:backup` (boolean) — Specifies if using PGP-encrypted keys, whether OpenBao should also store a plaintext backup of the said keys.
    * `:pgp_keys` (array) — Specifies an array of PGP public keys used to encrypt the output unseal keys.
    * `:require_verification` (boolean) — Enables verification which after successful authorization with the current unseal keys, ensures the new unseal keys are returned but the root key is not actually rotated.
    * `:secret_shares` (integer, required) — Specifies the number of shares to split the root key into.
    * `:secret_threshold` (integer, required) — Specifies the number of shares required to reconstruct the root key.
  """
  @operation "rotate-attempt-initialize-rotate-recovery-init"
  @spec rotate_attempt_initialize_rotate_recovery_init(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_attempt_initialize_rotate_recovery_init(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/rotate/recovery/init", opts,
      body: [:backup, :pgp_keys, :require_verification, :secret_shares, :secret_threshold],
      required: [:secret_shares, :secret_threshold]
    )
  end

  @doc ~S"""
  Reads the configuration and progress of the current root rotate attempt.

  `GET /v1/sys/rotate/root/init`

  Initialize, read status or cancel the process of the rotation of the root or recovery key.
  """
  @operation "rotate-attempt-read-progress"
  @spec rotate_attempt_read_progress(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_attempt_read_progress(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/rotate/root/init", opts, [])
  end

  @doc ~S"""
  Reads the configuration and progress of the current root rotate attempt.

  `GET /v1/sys/rotate/recovery/init`

  Initialize, read status or cancel the process of the rotation of the root or recovery key.
  """
  @operation "rotate-attempt-read-rotate-recovery-init"
  @spec rotate_attempt_read_rotate_recovery_init(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_attempt_read_rotate_recovery_init(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/rotate/recovery/init", opts, [])
  end

  @doc ~S"""
  Enter a single unseal key share to progress the rotation of the root key of OpenBao.

  `POST /v1/sys/rotate/root/update`

  Progress the rotation process by providing a single key share.

  ## Options

    * `:key` (string) — Specifies a single unseal key share.
    * `:nonce` (string) — Specifies the nonce of the rotation attempt.
  """
  @operation "rotate-attempt-update"
  @spec rotate_attempt_update(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_attempt_update(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/rotate/root/update", opts, body: [:key, :nonce])
  end

  @doc ~S"""
  Enter a single unseal key share to progress the rotation of the root key of OpenBao.

  `POST /v1/sys/rotate/recovery/update`

  Progress the rotation process by providing a single key share.

  ## Options

    * `:key` (string) — Specifies a single unseal key share.
    * `:nonce` (string) — Specifies the nonce of the rotation attempt.
  """
  @operation "rotate-attempt-update-rotate-recovery-update"
  @spec rotate_attempt_update_rotate_recovery_update(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_attempt_update_rotate_recovery_update(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/rotate/recovery/update", opts, body: [:key, :nonce])
  end

  @doc ~S"""
  Delete the backup copy of PGP-encrypted unseal keys.

  `DELETE /v1/sys/rotate/root/backup`

  Allows fetching or deleting the backup of the rotated unseal keys.
  """
  @operation "rotate-delete-backup-key"
  @spec rotate_delete_backup_key(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_delete_backup_key(server, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/rotate/root/backup", opts, [])
  end

  @doc ~S"""
  Delete the backup copy of PGP-encrypted unseal keys.

  `DELETE /v1/sys/rotate/recovery/backup`

  Allows fetching or deleting the backup of the rotated unseal keys.
  """
  @operation "rotate-delete-rotate-recovery-backup"
  @spec rotate_delete_rotate_recovery_backup(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_delete_rotate_recovery_backup(server, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/rotate/recovery/backup", opts, [])
  end

  @doc ~S"""
  Rotate the encryption key.

  `POST /v1/sys/rotate` — needs `sudo`

  Rotates the backend encryption key used to persist data.
  """
  @operation "rotate-encryption-key"
  @spec rotate_encryption_key(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_encryption_key(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/rotate", opts, [])
  end

  @doc ~S"""
  Return the backup copy of PGP-encrypted unseal keys.

  `GET /v1/sys/rotate/root/backup`

  Allows fetching or deleting the backup of the rotated unseal keys.
  """
  @operation "rotate-read-backup-key"
  @spec rotate_read_backup_key(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_read_backup_key(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/rotate/root/backup", opts, [])
  end

  @doc ~S"""
  Return the backup copy of PGP-encrypted unseal keys.

  `GET /v1/sys/rotate/recovery/backup`

  Allows fetching or deleting the backup of the rotated unseal keys.
  """
  @operation "rotate-read-rotate-recovery-backup"
  @spec rotate_read_rotate_recovery_backup(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_read_rotate_recovery_backup(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/rotate/recovery/backup", opts, [])
  end

  @doc ~S"""
  Rotate the encryption key.

  `POST /v1/sys/rotate/keyring` — needs `sudo`

  Rotates the backend encryption key used to persist data.
  """
  @operation "rotate-rotate-keyring"
  @spec rotate_rotate_keyring(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_rotate_keyring(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/rotate/keyring", opts, [])
  end

  @doc ~S"""
  Cancel any in-progress rotate verification operation.

  `DELETE /v1/sys/rotate/root/verify`

  Read status of, progress or cancel the verification process of the rotation attempt.
  """
  @operation "rotate-verification-cancel"
  @spec rotate_verification_cancel(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_verification_cancel(server, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/rotate/root/verify", opts, [])
  end

  @doc ~S"""
  Cancel any in-progress rotate verification operation.

  `DELETE /v1/sys/rotate/recovery/verify`

  Read status of, progress or cancel the verification process of the rotation attempt.
  """
  @operation "rotate-verification-cancel-rotate-recovery-verify"
  @spec rotate_verification_cancel_rotate_recovery_verify(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_verification_cancel_rotate_recovery_verify(server, opts \\ []) do
    ExBao.Operation.call(server, :delete, "sys/rotate/recovery/verify", opts, [])
  end

  @doc ~S"""
  Read the configuration and progress of the current rotate verification attempt.

  `GET /v1/sys/rotate/root/verify`

  Read status of, progress or cancel the verification process of the rotation attempt.
  """
  @operation "rotate-verification-read-progress"
  @spec rotate_verification_read_progress(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_verification_read_progress(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/rotate/root/verify", opts, [])
  end

  @doc ~S"""
  Read the configuration and progress of the current rotate verification attempt.

  `GET /v1/sys/rotate/recovery/verify`

  Read status of, progress or cancel the verification process of the rotation attempt.
  """
  @operation "rotate-verification-read-rotate-recovery-verify"
  @spec rotate_verification_read_rotate_recovery_verify(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_verification_read_rotate_recovery_verify(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/rotate/recovery/verify", opts, [])
  end

  @doc ~S"""
  Enter a single new key share to progress the rotation verification operation.

  `POST /v1/sys/rotate/root/verify`

  Read status of, progress or cancel the verification process of the rotation attempt.

  ## Options

    * `:key` (string) — Specifies a single unseal share key from the new set of shares.
    * `:nonce` (string) — Specifies the nonce of the rotation verification operation.
  """
  @operation "rotate-verification-update"
  @spec rotate_verification_update(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_verification_update(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/rotate/root/verify", opts, body: [:key, :nonce])
  end

  @doc ~S"""
  Enter a single new key share to progress the rotation verification operation.

  `POST /v1/sys/rotate/recovery/verify`

  Read status of, progress or cancel the verification process of the rotation attempt.

  ## Options

    * `:key` (string) — Specifies a single unseal share key from the new set of shares.
    * `:nonce` (string) — Specifies the nonce of the rotation verification operation.
  """
  @operation "rotate-verification-update-rotate-recovery-verify"
  @spec rotate_verification_update_rotate_recovery_verify(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def rotate_verification_update_rotate_recovery_verify(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/rotate/recovery/verify", opts, body: [:key, :nonce])
  end

  @doc ~S"""
  Seal the OpenBao instance.

  `POST /v1/sys/seal`

  Seals the OpenBao instance.
  """
  @operation "seal"
  @spec seal(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def seal(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/seal", opts, [])
  end

  @doc ~S"""
  Check the seal status of an OpenBao instance.

  `GET /v1/sys/seal-status` — needs no token

  Returns the seal status of the OpenBao instance.
  """
  @operation "seal-status"
  @spec seal_status(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def seal_status(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/seal-status", opts, [])
  end

  @doc ~S"""
  Provides information about the backend encryption key.

  `GET /v1/sys/key-status`

  Provides information about the specific namespace barrier encryption key.
  """
  @operation "status-encryption-key"
  @spec status_encryption_key(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def status_encryption_key(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/key-status", opts, [])
  end

  @doc ~S"""
  Cause the node to give up active status.

  `POST /v1/sys/step-down`
  """
  @operation "step-down-leader"
  @spec step_down_leader(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def step_down_leader(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/step-down", opts, [])
  end

  @doc ~S"""
  List ACL policies with detailed information.

  `GET /v1/sys/policies/detailed/acl`

  ## Options

    * `:list` (boolean) — List ACL policies
  """
  @operation "system-list-policies-detailed-acl"
  @spec system_list_policies_detailed_acl(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def system_list_policies_detailed_acl(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/policies/detailed/acl", opts,
      query: [:list],
      fixed_query: %{"list" => "true"}
    )
  end

  @doc ~S"""
  List ACL policies with detailed information.

  `GET /v1/sys/policies/detailed/acl/{name}`

  ## Arguments

    * `name` — The name of the policy. Example: "ops"

  ## Options

    * `:list` (boolean) — List ACL policies
  """
  @operation "system-list-policies-detailed-acl-name"
  @spec system_list_policies_detailed_acl_name(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def system_list_policies_detailed_acl_name(server, name, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/policies/detailed/acl/#{ExBao.Operation.escape(name)}",
      opts,
      query: [:list],
      fixed_query: %{"list" => "true"}
    )
  end

  @doc ~S"""
  Configure the values to be returned for the UI header.

  `POST /v1/sys/config/ui/headers/{header}` — needs `sudo`

  This path responds to the following HTTP methods. GET /<header> Returns the header value. POST /<header> Sets the header value for the UI. DELETE /<header> Clears the header value for UI. LIST / List the headers configured for the UI.

  ## Arguments

    * `header` — The name of the header.

  ## Options

    * `:multivalue` (boolean) — Returns multiple values if true
    * `:values` (array) — The values to set the header.
  """
  @operation "ui-headers-configure"
  @spec ui_headers_configure(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def ui_headers_configure(server, header, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/config/ui/headers/#{ExBao.Operation.escape(header)}",
      opts,
      body: [:multivalue, :values]
    )
  end

  @doc ~S"""
  Remove a UI header.

  `DELETE /v1/sys/config/ui/headers/{header}` — needs `sudo`

  This path responds to the following HTTP methods. GET /<header> Returns the header value. POST /<header> Sets the header value for the UI. DELETE /<header> Clears the header value for UI. LIST / List the headers configured for the UI.

  ## Arguments

    * `header` — The name of the header.
  """
  @operation "ui-headers-delete-configuration"
  @spec ui_headers_delete_configuration(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def ui_headers_delete_configuration(server, header, opts \\ []) do
    ExBao.Operation.call(
      server,
      :delete,
      "sys/config/ui/headers/#{ExBao.Operation.escape(header)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Return a list of configured UI headers.

  `GET /v1/sys/config/ui/headers`

  This path responds to the following HTTP methods. GET /<header> Returns the header value. POST /<header> Sets the header value for the UI. DELETE /<header> Clears the header value for UI. LIST / List the headers configured for the UI.
  """
  @operation "ui-headers-list"
  @spec ui_headers_list(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def ui_headers_list(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/config/ui/headers", opts,
      fixed_query: %{"list" => "true"}
    )
  end

  @doc ~S"""
  Return the given UI header's configuration

  `GET /v1/sys/config/ui/headers/{header}` — needs `sudo`

  This path responds to the following HTTP methods. GET /<header> Returns the header value. POST /<header> Sets the header value for the UI. DELETE /<header> Clears the header value for UI. LIST / List the headers configured for the UI.

  ## Arguments

    * `header` — The name of the header.
  """
  @operation "ui-headers-read-configuration"
  @spec ui_headers_read_configuration(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def ui_headers_read_configuration(server, header, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/config/ui/headers/#{ExBao.Operation.escape(header)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Unseal the OpenBao instance.

  `POST /v1/sys/unseal` — needs no token

  Unseals the OpenBao instance.

  ## Options

    * `:key` (string) — Specifies a single unseal key share. This is required unless reset is true.
    * `:reset` (boolean) — Specifies if previously-provided unseal keys are discarded and the unseal process is reset.
  """
  @operation "unseal"
  @spec unseal(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def unseal(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/unseal", opts, body: [:key, :reset])
  end

  @doc ~S"""
  Unwraps a response-wrapped token.

  `POST /v1/sys/wrapping/unwrap`

  ## Options

    * `:token` (string) —
  """
  @operation "unwrap"
  @spec unwrap(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def unwrap(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/wrapping/unwrap", opts, body: [:token])
  end

  @doc ~S"""
  Provide an unseal key share for root token generation.

  `POST /v1/sys/generate-root-token/update`

  Reads status, initializes, or cancels a root token generation process.

  ## Options

    * `:key` (string, required) — Unseal key share.
    * `:nonce` (string, required) — Nonce for the generation operation.
  """
  @operation "update-generation-attempt-root-token"
  @spec update_generation_attempt_root_token(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def update_generation_attempt_root_token(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/generate-root-token/update", opts,
      body: [:key, :nonce],
      required: [:key, :nonce]
    )
  end

  @doc ~S"""
  Returns map of historical version change entries

  `GET /v1/sys/version-history`

  List historical version changes sorted by installation time in ascending order.
  """
  @operation "version-history"
  @spec version_history(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def version_history(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/version-history", opts,
      fixed_query: %{"list" => "true"}
    )
  end

  @doc ~S"""
  Delete a workflow.

  `DELETE /v1/sys/workflows/manage/{path}`

  Create, read, update and delete workflows.

  ## Arguments

    * `path` — Path of the workflow.
  """
  @operation "workflows-delete-workflows-manage-path"
  @spec workflows_delete_workflows_manage_path(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def workflows_delete_workflows_manage_path(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :delete,
      "sys/workflows/manage/#{ExBao.Operation.escape_path(path)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Execute the given workflow.

  `POST /v1/sys/workflows/execute/{path}`

  Execute a workflow.

  ## Arguments

    * `path` — Path of the workflow.
  """
  @operation "workflows-execute-write-workflows-execute-path"
  @spec workflows_execute_write_workflows_execute_path(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def workflows_execute_write_workflows_execute_path(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/workflows/execute/#{ExBao.Operation.escape_path(path)}",
      opts,
      []
    )
  end

  @doc ~S"""
  List workflows.

  `GET /v1/sys/workflows/manage`
  """
  @operation "workflows-list-workflows-manage"
  @spec workflows_list_workflows_manage(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def workflows_list_workflows_manage(server, opts \\ []) do
    ExBao.Operation.call(server, :get, "sys/workflows/manage", opts,
      fixed_query: %{"list" => "true", "scan" => "true"}
    )
  end

  @doc ~S"""
  Retrieve a workflow.

  `GET /v1/sys/workflows/manage/{path}`

  Create, read, update and delete workflows.

  ## Arguments

    * `path` — Path of the workflow.

  ## Options

    * `:list` (string) — Return a list if `true`
    * `:scan` (string) — Return a recursive list if `true`
  """
  @operation "workflows-read-workflows-manage-path"
  @spec workflows_read_workflows_manage_path(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def workflows_read_workflows_manage_path(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :get,
      "sys/workflows/manage/#{ExBao.Operation.escape_path(path)}",
      opts,
      query: [:list, :scan]
    )
  end

  @doc ~S"""
  Execute the given workflow.

  `POST /v1/sys/workflows/trace/{path}`

  Execute a workflow.

  ## Arguments

    * `path` — Path of the workflow.
  """
  @operation "workflows-trace-write-workflows-trace-path"
  @spec workflows_trace_write_workflows_trace_path(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def workflows_trace_write_workflows_trace_path(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/workflows/trace/#{ExBao.Operation.escape_path(path)}",
      opts,
      []
    )
  end

  @doc ~S"""
  Create or update a workflow.

  `POST /v1/sys/workflows/manage/{path}`

  Create, read, update and delete workflows.

  ## Arguments

    * `path` — Path of the workflow.

  ## Options

    * `:after` (string) — Optional entry to begin listing after; not required to exist.
    * `:allow_unauthenticated` (boolean) — Whether this workflow can be executed unauthenticated. Use with care.
    * `:cas` (integer) — Check and set version of the workflow.
    * `:cas_required` (boolean) — Whether to require check and set for modifying this workflow.
    * `:description` (string) — Workflow description.
    * `:limit` (integer) — Optional number of entries to return; defaults to all entries.
    * `:workflow` (string, required) — Workflow definition in either HCL or JSON format.
  """
  @operation "workflows-write-workflows-manage-path"
  @spec workflows_write_workflows_manage_path(ExBao.server(), String.t(), keyword()) ::
          ExBao.Operation.result()
  def workflows_write_workflows_manage_path(server, path, opts \\ []) do
    ExBao.Operation.call(
      server,
      :post,
      "sys/workflows/manage/#{ExBao.Operation.escape_path(path)}",
      opts,
      body: [:after, :allow_unauthenticated, :cas, :cas_required, :description, :limit, :workflow],
      required: [:workflow]
    )
  end

  @doc ~S"""
  Response-wraps an arbitrary JSON object.

  `POST /v1/sys/wrapping/wrap`
  """
  @operation "wrap"
  @spec wrap(ExBao.server(), keyword()) ::
          ExBao.Operation.result()
  def wrap(server, opts \\ []) do
    ExBao.Operation.call(server, :post, "sys/wrapping/wrap", opts, [])
  end

  # ── end of generated ──
end
